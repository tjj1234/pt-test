# Slice 0 · 冒烟清单（需真 PT key + 真 DSH + 浏览器，手动跑）

> 自动回归覆盖不了的路径，落到这里。每项标注「验证点 → 期望结果」。
> 跑完如实勾选 ✅ / ❌，❌ 项回到对应 Bug 的修复待办。

## A. 认证与会话
- [ ] 注册成功 → 落到用户表，返回会话
- [ ] 重复用户名 → 拒绝
- [ ] 错误密码登录 → 拒绝
- [ ] 登录后 Cookie 带 HttpOnly/SameSite → 浏览器 devtools 可见
- [ ] 登出后旧 Cookie 再访问 → 401
- [ ] 连续错密码 → 触发防爆破锁定

## B. PT key 与密钥安全
- [ ] 存 key → 落库为密文（直接查库看不到明文）
- [ ] 设置页只显示末 4 位
- [ ] live verify 开 → 假 key 被拒；关 → 退化为本地格式校验
- [ ] 日志/返回体不含完整 key

## C. 对话与 SSE（真 DSH）
- [ ] 发消息 → `delta` 逐字到达且顺序正确
- [ ] 工具调用 → `step` 出现
- [ ] 结束 → `done` 最终到达
- [ ] DSH 报错 → 前端收到统一错误（非原始堆栈）
- [ ] 未绑 key → 友好提示「先保存 key」
- [ ] 停止 → 返回 `stopped`，且后端进程不退出（可继续发下一问）
- [ ] 分岔/重新生成 → 行为与之前一致

## D. 工作区 / 租户隔离
- [ ] 用户 A 看不到用户 B 的对话（URL 直访 → 404）
- [ ] 上传文件路径含 `../` → 被拒
- [ ] 看板租户头伪造 → 被忽略（租户只从 token 派生）

## E. 归因 / Webhook
- [ ] 正确 token 读归因 → 200
- [ ] 错误/过期 token → 拒绝
- [ ] event_id 重放 → 不重复计数
- [ ] 非法事件 → 拒绝

## F. B1 粘贴图片（浏览器实测，重点）
- [ ] Chrome Ctrl+V 粘贴截图 → 出现图片预览
- [ ] Firefox Ctrl+V 粘贴 → 出现图片预览
- [ ] Safari 粘贴 → 出现图片预览
- [ ] 无剪贴板图片时 Ctrl+V → 走正常文本粘贴
> 记录：哪些浏览器成功、哪些失败、失败时控制台报什么（这是定位 B1 的关键证据）。

## G. B7 图片真实读取（真 LLM + 视觉模型）
- [ ] 视觉模型 + 上传图片 → 回复内容确实「看到」了图片
- [ ] 非视觉模型 + 上传图片 → 上传前就被友好拒绝，而不是「找不到图片」
- [ ] 记下 `seed-2-0-mini-260215` 在 `/api/models` 返回里的能力声明（是否 vision）
