# 北极星项目 · 唯一总文档 v1.9

> ## 📖 这份文档怎么用
>
> **这是全项目唯一的事实来源。** 任何时候想知道"项目到哪了、为什么变成现在这样、有哪些文档、哪些还没做"，
> **只读这一份就够**，不需要再去翻别的文档。
>
> **三层结构**：
> - **第一层 · 时间线** —— 项目怎么一步步变成现在这样，**每次变化为什么**，影响了哪些功能和文件
> - **第二层 · 成果线** —— 当前所有**最新有效**的文档清单（名称 / 作用 / 最后更新时间）
> - **第三层 · 进度线** —— 已完成 / 进行中 / 待做
>
> **第四层 · 全量信息模块** —— 其他所有信息（架构、数据模型、API 契约、已知缺陷、铁律、术语、命令、待拍板）
>
> **维护规则**：
> 1. 只有这一份进度类文档，其余进度文档全部作废
> 2. 每次有实质变化，就发一个新版（`v1.1`、`v1.2`…），**旧版不再引用**
> 3. 更新时**必须同时更新三层**（新事件进时间线、新文档进成果线、状态进进度线）
> 4. ★ **发布前必须做「自洽性自查」**（见铁律 #14）
>
> **本版（v1.8）相对 v1.7 的变化**：
> ① §3.3b 生产发布缺口清单：P1 #5/#7/#8/#9/#10/#11/#12/#13/#14 全部标「✅ 已补齐」；#6 标「✅ 已实现（A 方案默认关，数据侧待接入）」；#10 标「✅ 配置模板已出（域名/证书待用户填）」；新增 P1-1「反代后 IP 塌缩」标「✅ 已修」；P2 #15-18 仍「待做」
> ② 时间线新增 4 条（#50-#53）：key 校验 / 运维-部署观测 / 数据安全 / 看板隔离，每条带「实现者 agent / 验收者 agent / 结论」
> ③ 汇总 P1 验收发现的全部 P2 非阻塞项；新增「诚实边界 / 待用户提供」醒目区块
>
> 建立时间：2026-09-14 ｜ 取代：`北极星实现任务书与进度-v1.44.md` 及全部历史版本、`DSH基座-*` 系列进度文档、`北极星总进度-v2.0.md`

---

# 第一层 · 时间线

> **这一层回答："项目怎么变成现在这样的？每次为什么变？"**
> 重点不是"做了什么"，而是 **"为什么这么做"** —— 因为下次遇到类似判断时需要依据。

## 1.1 阶段总览

| 阶段 | 时间 | 做了什么 | 结果 |
|---|---|---|---|
| **① 设计阶段** | ~09-09 | 把产品需求拆成设计单元（`北极星开发/`） | 设计文档体系建立 |
| **② 实现阶段** | 09-10 ~ 09-11 | 20 个实现单元，逐个独立验收 | 全部通过 + **抓到 11 个真问题** |
| **③ 交付版阶段** | 09-11 | 打包可运行交付版 + 端到端自检 | 交付版跑通（8095） |
| **④ DSH 基座阶段** | 09-14 | 产品形态决策、技能接真数据、MCP 接线 | 见下方详细 |
| **⑤ X 平台补齐 + 收口阶段** | 09-14 | X 平台补丁 + 验收、进度文档收口、部署门禁核查 | X ✅通过（**补丁待落地**）；门禁阻断 6 项 |
| **⑥ 产品壳 + 多租户规划** | 09-14 ~ 09-15 | 业务壳建成（形态 B）、看板打通、多租户方案定案 | 业务壳 25/25；看板出数；多租户 BYOK 定案待实现 |

## 1.2 详细时间线

| # | 时间 | 事件 | **为什么这样变** | 影响的功能 | 影响的文件 |
|---|---|---|---|---|---|
| 1 | 09-09 | 设计阶段完成，界定本期范围 | 要先把"做什么/不做什么"钉死，否则实现期无限蔓延 | 全局 | `北极星范围-本期-v1.2.md` |
| 2 | 09-10 | 数据接入层 1.1~1.5 全部完成 | 里程碑：**这是后面积累数据的地基** | 事件接入、查询、审计 | `backend/src/collect/*`、`backend/src/events/query.ts`、`backend/db/001_init.sql` |
| 3 | 09-10 | **单元 2.1 验收抓到高危：只读代码留写工具后门** | 声称"只读"，但代码里留有能改广告的路径 | 广告数据接入 | `backend/src/ads/ryze-client.ts` |
| 4 | 09-10 | 单元 2.2 时区缺陷 | 只用 `opts.timezone`，缺失时**静默回退 UTC** → 广告数据与用户行为差 9 小时，对不上 | 广告落库、归因 | `backend/src/ads/ingest.ts` |
| 5 | 09-10 | 单元 2.3 缺归因索引 + 折算漏标 | 性能与数据口径问题 | JOIN 归因 | `backend/src/ads/attribution.ts` |
| 6 | 09-11 | **单元 4.1 第 1 次验收不通过（高危：纸面合规）** | ★ **验收者去查了 DSH 源码，发现 `@deepseek-ai/dsh-mcp-client` 的 `syncTools` 会无条件注册 server 全部工具，且配置 schema 里没有 allowlist 字段**。→ 原方案"DSH 直连 Ryze + 配置声明白名单"**根本不成立**，AI 实际有权改广告预算 | AI 层工具权限 | 方案重做：引入**后端只读代理** `backend/src/ads/mcp-proxy.ts` |
| 7 | 09-11 | 单元 4.1 第 2 次验收不通过 | 废弃的直连配置 `mcp-ryze-config.json` **仍占着规范文件名**，误载即复现旧洞 | AI 层安全 | `北极星实现/ai/mcp-ryze-config.json` |
| 8 | 09-11 | 单元 4.1 第 3 次验收通过（**有条件：5 条部署门禁**） | — | — | 见第三层 |
| 9 | 09-11 | 单元 2.4 历经 **5 次**验收/返工才通过 | 每次不通过都拦下一个真问题 | Funnel API | `backend/src/ads/funnel.ts` |
| 10 | 09-11 | 单元 3.1 抓到：**修好了 bug 但 `index.html` 没接线** | 修正版文件写好了，却没人把它接上 → 4 个 Tab 的三态提示**看不见** | 看板界面 | `frontend/index.html`、`frontend/app.fixed.js` |
| 11 | 09-11 | 单元 3.2 抓到：**排序方向反（null 浮顶）** | 默认按 ROI 降序时，"无法归因"的行**浮到表格最顶部** → 运营误以为它排行靠前。**数字没假，结论是错的** | 素材 Tab | `frontend/tabs/creatives.fixed.js`、`FIX-3.2-排序与空值.md` |
| 12 | 09-11 | 单元 3.3/3.4 抓到 **3 处空值当 0** | 「算不出来」被显示成「0」 | 用户/产品/健康 Tab | `FIX-空值口径专项.md` |
| 13 | 09-11 | **单元 3.4 抓到：审计功能根本没接出 HTTP 路由** ← 跨单元视野 | 单元验收天然看不见"单元之间的缝" | 健康 Tab 恒 404 | `backend/src/audit/route.ts` |
| 14 | 09-11 | **3.2 复验者抓到：我的文档「虚假保证」** | 文档声称"门禁会拦"，实际门禁**没拦**（纸面合规，这次是我犯的） | 门禁机制 | `scripts/preflight-*.mjs` |
| 15 | 09-11 | **顶层 agent 对照发现：后端无统一入口，3 个 API 无法同源访问** | 系统**装不起来** | 部署 | `backend/src/server.ts`（新增统一入口） |
| 16 | 09-11 | 前后端 API 路径一致性检查 | 自查补做（门禁未覆盖的维度） | 全局 | — |
| 17 | 09-11 | 前端 XSS 安全审查 + 后端 SQL 注入审查 | 自查补做（门禁未覆盖的维度），两个维度均通过 | 全局 | — |
| 18 | 09-11 | 交付版打包完成（8095） | 让 PM 能亲手点 | 全部 | `北极星实现/交付版/` |
| 19 | 09-11 | 交付版独立验收 + v1.45 返工 | 交付版层面又发现缺陷 6/7 | 交付版 | `北极星交付版-v1.45-独立验收问题与返工记录.md` |
| 20 | **09-14** | **产品形态拍板：形态 B** = `dsh --profile headless` + **自建业务壳**（不是直接发 DSH 的 Web UI） | 要卖给客户的是"北极星产品"，不是"一个 AI 工具" | 部署形态 | `DSH基座-部署与隔离决策-形态B.md` |
| 21 | 09-14 | **隔离决策**：产品 DSH 绝不与开发者的 DSH 共用 home | 否则**会话、配置、技能互相污染**。且 DSH 硬规则**拒绝 `--host 0.0.0.0`**（原话"会把远程代码执行暴露到网络上"）—— 有意为之，不许绕 | 部署形态 | 同上 |
| 22 | 09-14 | **技能 2「埋点质量」实现**（`tracking-quality`） | 审计系统只看 SS-GTM 侧 3 个事件，**PT 业务侧是另一套 6 事件**，「埋没埋齐」只有这个技能会查 | AI 层技能 | `.dsh/skills/tracking-quality/` |
| 23 | 09-14 | **技能 2 独立验收抓到高危：鉴权失败被伪装成"没数据"** | token 写错时后端返回 401，技能却输出「暂无可审计数据」+ **退出码 0** → **用户以为"系统里没数据"，真相是"凭证错了"** | 技能 2 | `adapter/main.py`（`BackendAuthFailed`） |
| 24 | 09-14 | 技能 2 返工 6 条 + 独立复验 **6/6 通过** | 复验方还做了**破坏性测试**（故意改坏代码，确认测试真的会失败） | 技能 2 | `test/smoke.py`（84 断言） |
| 25 | 09-14 | **4.1 MCP 接线：发现代理是"孤儿模块"** | `mcp-proxy.ts` 写完了，但**全项目没有任何文件 import 它**，也从没启动过 | AI 层工具链路 | `backend/src/ads/mcp-proxy.ts` |
| 26 | 09-14 | **破译 DSH 真实装配机制** | 设计文档假设配置写在 `settings.yaml` 的 `mcp:` 段 —— **是错的**。真实机制是 **`cordis.patch.yml` 分层叠加**（base → surface → profile 自己的 patch → `--patch` 覆盖层） | 部署形态 | `$DSH_HOME/profiles/*/cordis.patch.yml` |
| 27 | 09-14 | **4.1 接线完成 + 独立验收通过（三条红线独立复现）** | 用**替身上游**验机制（真 Ryze 本机不存在） | AI 层工具链路 | `ai/cordis.patch.ryze.yml`、`backend/standin-upstream.cjs` |
| 28 | 09-14 | 验收方证伪 **"报警器是活的"** | 「永远不响的报警器和没有报警器一样」→ 必须先证明检测机制会触发 | 验收方法 | 见铁律 #9 |
| 29 | 09-14 | **发现 X 平台缺陷**：前端有 X、后端不认 | PM 指出**他们还在投 X**，所以我原本"去掉筛选项"的建议是错的 → **后端实现缺口**，该补不该删 | 全数据链路 | 见单元 X |
| 30 | 09-14 | **发现部署门禁阻断发布（6 项）** | 门禁脚本 09-10 就写好了，**没人跑过** | 部署 | `scripts/preflight-deploy-v2.mjs` |
| 31 | 09-14 | **进度文档收口** | 进度散在两套文档里且主文档过期（v1.44 停更于 09-11，且 §2 与 §4 **自相矛盾**） | 项目管理 | `北极星总文档-v1.0.md` |
| 32 | 09-14 | **发现总文档自身有 3 处错误**（PM 抓到 1 处） | ★ **PM 拿着文档对照实物，发现自我引用写错** → 顺带自查又找出 2 处（已过期清单漏登记、范围写法会被误读） | 项目管理 | 见铁律 #14 |
| 33 | **09-14** | **单元 X 独立验收通过** | 验收方**构造了"只改白名单"的反例版本**，实测证实第 4 处改动确属必要 | 全数据链路 | `单元X独立验收报告-a7f3kq.md` |
| 34 | 09-14 | **业务壳建成（形态 B）** | 产品对外的"门"：登录 + 对话 + 看板导航，用户不碰 DSH | 产品入口 | `北极星产品/app/server.cjs`、`start.cjs` |
| 35 | 09-14 | **看板代理打通** | 业务壳原来 `/api/analytics/*` 全 404，补代理后看板出真数（4830 访问） | 归因看板 | `北极星产品/app/server.cjs` |
| 36 | 09-14 | **技能 6 策略建议验收通过** | 真实查询给出 $13,590.03 / 0.34× / 18.3🔴，诚实声明"未补数未编造" | 技能 6 | `.dsh/skills/ad-strategy/` |
| 37 | 09-15 | **★ PM 发现多租户缺失** | 产品 `.credentials.yaml` 与开发者**逐字节相同** → 任何人登录都花开发者的 PT key；单账号、会话存内存 | 全局 | `北极星产品/运行时/dsh-home/.credentials.yaml` |
| 38 | 09-15 | **多租户方案定案** | BYOK（用户自带 key）+ AES-256-GCM 加密落盘 + 产品级；主密钥 DPAPI | 全局 | `北极星产品/架构-多租户与BYOK.md` |
| 39 | 09-15 | **问题与缺口总清单** | 把现存问题 + 缺口盘成一份清单供 PM 审阅 | 项目管理 | `北极星-问题与缺口总清单-v1.0.md` |
| 40 | 09-15 | **归因面板 4 模块新需求** | PM 指出总览页缺：调用渠道 / 全局模型明细 / 国家漏斗对比 / 国家下钻 | 归因看板 | 参考 `base/merged-prototype/attribution/index.html` |
| 41 | 09-15 | **他方 AI 方案评审** | 借鉴"两层映射 + 数据层 RLS 已实现"，纠正"把读数据 token 当模型 key"、"workspace 不用做" | 多租户设计 | 已并入本方案 |
| 42 | 09-15 | **门禁 v3 被发现会漏检** | 修 [7] 引入假阴性：`const u="a//b"` 同行写法漏检真缺陷；[7] 查的是没被加载的文件 | 部署门禁 | `scripts/preflight-deploy-v3.mjs` |
| 43 | 09-15 | **门禁四轮迭代至 v7 通过** | v4→v5→v6→v7 每轮独立证伪，最终通过（剩余为文档化边界）；验收拦住「自家代理被误判直连」等生产级 bug | 部署门禁 | `scripts/preflight-deploy-v7.mjs` |
| 44 | 09-15 | **X 源码化验收通过** | 6 处补丁落源码（含第 4 处泛化逐字节保住 google/meta），逐行 diff + tsc + transpile 三重验证 | X 平台 | `backend/src-x` / `db-x` / `lib-x` |
| 45 | 09-15 | **归因 4 模块通过** | 调用渠道/模型明细接真实 `by_source`/`by_model`（55+54=109 自洽）；广告侧无数据空处理；零编造 | 归因面板 | `归因4模块/overview-modules.js` |
| 46 | 09-15 | **多租户 U1~U5 全部收口** | 身份库(修提权)+key加密(DPAPI)+租户隔离(修假stub)+前端(修口径)+收口(修死代码)，五单元独立验收全过 | 多租户 | `北极星产品/app/server-v2.cjs` |
| 47 | 09-15 | **PM 指出「demo 级」偏差，做生产缺口自查** | 缺注册页/设置入口/needKey提示/会话持久化/看板隔离等 18 项，入本文档 §3.3b | 产品 | §3.3b 生产发布缺口清单 |
| 48 | 09-15 | **P0 基础用户页面补齐** | 注册(tab+确认密码+自动登录跳设置) + 「我的Key」入口(红点/提示条) + needKey友好提示；独立验收通过(29/29+端到端) | 前端 | `北极星产品/P0页面-落地说明.md` |
| 49 | 09-15 | **P1 服务端硬化（#5/#8/#9）** | 会话落库重启不丢 + 登录暴力破解防护 + API限流(login/register/chat)；独立验收通过(43/43+独立17项) | 后端 | `北极星产品/P1硬化-落地说明.md` |

### 1.2 续 · P1 收尾批次（v1.8 新增 · 编号接 #49）

> ★ **铁律 #1：实现者 ≠ 验收者**，每条都标注双方 agent；独立验收通过才算 done。
> 验收不通过的，如实记录「不通过 → 修复 → 复核通过」。

| # | 时间 | 事件 | 实现者 agent | 验收者 agent | 结论 |
|---|---|---|---|---|---|
| 50 | 09-16 | **P1 #7 key 有效性校验**：保存 PT key 从「直接加密落库、不验真伪」改为「先验证、通过才落库、不过给中文提示」。实现 = 本地格式校验（fail-fast）+ 对 PowerTokens 做一次最小代价 live 鉴权探测（`GET /v1/models` + Bearer；**真 key→200 / 假 key→500 已实测**，非 2xx fail-closed 拒绝落库）。 | 实现 agent（实现单元） | 独立验收 agent（≠ 实现者） | ❌ 不通过 → 修复 → ✅ 复核通过 |
| 51 | 09-16 | **P1 #10/#11/#13 运维-部署观测**：`logging.cjs` 结构化 JSON 日志（三层零泄露防线）+ `server-v4.cjs`（日志接线 + `/healthz` 探活）+ `Dockerfile` + PM2(`ecosystem.config.js`) + `nginx.conf.example`(TLS 模板) + 部署说明四套。真实自测 **31/31**。 | 实现 agent（实现单元） | 独立验收 agent（≠ 实现者） | ✅ 通过（31/31，零泄露实锤；**抓出 1 项 P1 反代 IP 塌缩，并入收尾**） |
| 52 | 09-16 | **P1 #12/#14 数据安全**：`backup.cjs`/`restore.cjs` 冷备份/还原（rename 探测拒热拷贝 + 还原后 SELECT 1 校验）+ `key-migrate.cjs`/`key-import.cjs` master key 迁移（PBKDF2-SHA256 600k 次 → AES-256-GCM，目标机 DPAPI 重落盘）。真实自测 **26/26**（真实 PGlite + DPAPI）。 | 实现 agent（实现单元） | 独立验收 agent（≠ 实现者） | ✅ 通过（26/26；**2 项 P2 非阻塞**） |
| 53 | 09-16 | **P1-1 + #6 看板隔离**：`ratelimit-v2.cjs` 加 `TRUST_PROXY` 开关 + 信任边界（修反代 IP 塌缩）+ `auth-routes-v4.cjs` + `server-v6.cjs` + `start-x-v2.cjs` A 方案租户头注入（默认关）。 | 实现 agent（实现单元） | 独立验收 agent（≠ 实现者） | ❌ 不通过 → 修复 → ✅ 复核通过（14/14，演示租户数据不断、随机租户零泄漏） |

**#50 与 #53 的「不通过 → 修复 → 复核通过」明细（如实记录）**：

| # | 首验抓到的问题 | 修复 | 复核结论 |
|---|---|---|---|
| 50 | ① P0：`key-routes-v3.cjs` 只在自测里用 require shim 接上，**真实入口未接线** ② P1：live 探活默认开有风险（真实 key 对 `/v1/models` 是否稳定 2xx 未实测确认） | 接线进 `server-v5.cjs`（真实入口替换 require）；live 探活保留可单点替换钩子 + `PT_KEY_LIVE_VERIFY=0` 退化开关 | ✅ 复核通过（真 key→200/假 key→500 已实测；fail-closed 保留） |
| 53 | **P1 跨租户泄漏**：未知租户走 funnel 时，`resolveWorkspaceId` 回落演示工作区 → 泄漏演示租户的 `ads_synced_through`（"碰巧不泄露"而非"结构性隔离"） | `start-x-v4.cjs` + `lib\deps-x-v4.cjs`（`createWorkspaceResolver` **去 fallback、未知租户返回 null**）+ `.dist-x-v4\ads\funnel.js`（`queryFreshness` 对 workspaceId=null 短路）；启动新增 3.5 步幂等兜底写演示租户映射 | ✅ 复核通过（14/14：演示租户 groups=7/roi=7/ads_synced_through 有值、数据不断；随机租户全空且 `ads_synced_through===null`） |

## 1.3 ★ 关键转折点（几个"为什么"值得深挖）

### 转折点 1：为什么引入"后端只读代理"？

**起因**：4.1 第 1 次验收不通过。验收者去读了 DSH 源码，发现：

> `@deepseek-ai/dsh-mcp-client` 的 `syncTools` **无条件注册 server 全部工具**，配置 schema 里**没有 allowlist 字段**。

**推演**：若 DSH 直连 Ryze，Ryze 那边 **150+ 个工具（含大量写工具）会全部出现在 AI 手里**。
设计文档里那句「白名单」在原生插件下**只是纸面声明**，没有任何执行力。

**决策**：DSH **不直连 Ryze**，改连**我方自己写的只读代理**（`backend/src/ads/mcp-proxy.ts`），
**白名单在执行层收口** —— `tools/list` 只回白名单、`tools/call` 白名单外绝不转发。

**验证方式（这是重点）**：不是"看代码写了没有"，而是
- 硬调写工具名 → 必须得 **`Error: unknown tool`**（注册表里根本没它 = "结构上不存在"，而不是"存在但被拒"）
- 上游替身**收到的写工具次数 = 0**
- 且**先证明报警器是活的**，才能说"它一次都没响"

### 转折点 2：为什么产品形态选"形态 B"？

**备选**：A = 直接发 DSH 的 Web UI；B = **自建业务壳**（DSH 调用层早期为 `dsh --profile headless` 一次性 spawn；**2026-09-17 起迁移为「常驻 agent（SDK 模式）」**，见 §4.8）。

**选 B 的原因**：要卖的是"**北极星产品**"，不是"一个通用 AI 工具"。
DSH 的 Web UI 暴露的是 DSH 自己的形态，不是北极星的业务形态。

> ★ **2026-09-17 演进**：业务壳定位不变，DSH 调用层从「每次 spawn 一次性 headless」迁移为「**常驻 agent（SDK 模式）**」——每租户一个常驻 DSH 进程，冷启动从 ~6s 降到 ~300ms。详见 §4.8 与 v1.10 增量。

**连带的硬约束**：DSH 默认把**所有用户数据放在同一个根目录**下 → 产品 DSH 必须**独立 home**，否则与开发者实例互相污染。

### 转折点 3：为什么"技能 2 的鉴权问题"是最值得记的一个？

**现象**：token 写错时，技能输出「暂无可审计数据，请先完成 Workflow2 最小审计」，**退出码 0**。
**真相**：后端实际返回 `HTTP 401 {"code":"UNAUTHORIZED"}`。

**为什么它值钱**：
- **我自己的冒烟测试 60 条全绿，没抓到它** —— 因为我**只测了底层函数，没测用户真正走的那条路**
- **用户会以为"系统里没数据"**，而真相是"你的凭证错了" —— **一个错误的结论，比一个报错更危险**
- 抓它的方法："**断言必须从用户真正走的入口驱动**"

### 转折点 4：为什么 X 平台要"补"而不是"删"？

**我一开始的判断（错的）**：数据库 `CHECK (platform IN ('meta','google'))` 让 X 永远查不出来 → 建议把「X-Ads」从筛选栏去掉。

**PM 的反驳**：**我们还在投 X。**

**正确的推理**：前后端不一致时，**不许默认"多出来的那部分"是错的**。
实测 X 占 **~15.5%** 流量（693 访问 / 175 注册 / 12 充值 / **$1,188**），
而且 X 的种子数据是**故意设计的归因边界用例**：
- `pt-brand`/`img_brand_awareness`/JP → **有花费无充值**
- `pt-launch-us`/`orphan_creative_zz`/SG → **无广告实体 → 未命中**

**结论**：后端不支持 X = **这两个归因边界场景根本跑不起来**。要**补后端**，不是删筛选项。

### 转折点 5：为什么"独立验收"机制值得保留？（战果统计）

**11 个真问题里，没有一条是「代码跑不起来」** —— 全部是「**能跑，但会出事**」：

- 跑得起来，但**算出来的结论是错的**（排序 null 浮顶、空值当 0、时区差 9 小时）
- 跑得起来，但**该看不见的东西看不见**（Tab 没接线、审计没路由、无统一入口）
- 跑得起来，但**该拦住的没拦住**（只读后门、纸面合规、虚假保证）

> **推论**：如果只做"能不能跑"的验收（跑一下、不报错、就通过），
> **这 11 个问题一个都抓不到**，而它们每一个都会在线上变成真实的错误决策或安全事件。

**发现问题靠什么**：

| 手段 | 抓到数量 | 说明 |
|---|---|---|
| 单元独立验收（20 次） | 8 | 主力机制 |
| 验收者的「跨单元视野」 | 1 | 3.4 验收者顺手看了后端路由 |
| 验收者对**交付文档**的核查 | 1 | 3.2 复验者去读了门禁源码 |
| 顶层 agent 前后端对照 / 补做检查 | 1 | 无统一入口 |
| **PM 拿文档对照实物** | 1 | 总文档自我引用写错（v1.1 修正） |

**三点启示**：① 单元验收是主力，但**看不见单元之间的缝**；② **"不信声明、只看实物"** 才能抓到纸面合规；③ **"集成检查"必须独立成一环**。

### 转折点 6：为什么验收方要"自己造一个反例版本"？

**单元 X 的验收方做了一件很对的事**：实现者声称"任务书漏了第 4 处改动"（`attribution.ts`）。
验收方**没有直接信，也没有直接否**，而是**自己构造了一个"只改前 3 处"的版本**（`.dist-x3`）跑了一遍：

> 实测 `platform=x` → **HTTP 200，但分组 = 0**

**结论**：实现者的说法成立 —— 只改 3 处会得到"**看起来成功、其实没数据**"的假成功。

**这就是"试图证伪"的正确姿势**：不接受论断，也不无理由怀疑，而是**构造反例去试**。

---

# 第二层 · 成果线

> **这一层回答："现在有哪些东西？哪些文档还有效？"**
> 规则：**只列当前有效的最新版本**。已过期的在 §2.4 单列。

## 2.1 代码成果（当前有效）

| 位置 | 作用 | 状态 |
|---|---|---|
| `北极星实现/backend/src/` | 后端源码（TS） | 有效 |
| `北极星实现/backend/db/001_init.sql` | 数据库初始化（建表 + RLS + 自检） | 有效 |
| `北极星实现/backend/db/003_add_x_platform.sql` | **X 平台迁移**（放宽 5 表 CHECK） | ⚠️ **已产出，未应用** |
| `北极星实现/backend/src/ads/mcp-proxy.ts` | **MCP 只读代理**（白名单执行层） | ✅ 已接线并验收 |
| `北极星实现/backend/src/ads/ryze-client.ts` | Ryze 客户端 + **工具三表** | 有效（**X 待补**） |
| `北极星实现/ai/cordis.patch.ryze.yml` | **DSH 接线配置**（交付物） | ✅ 新增 |
| `北极星实现/backend/standin-upstream.cjs` | 替身上游 MCP server（兼报警器） | 验证用 |
| `北极星实现/backend/start-standin-proxy.cjs` | 代理启动脚本 | ✅ 新增 |
| `北极星实现/交付版/` | **可运行交付版**（8095） | ✅ 在跑（**X 仍 400**） |
| `北极星实现/交付版/.dist-x` | **打过 X 补丁的编译产物**（验证用） | 验证通过 |
| `.dsh/skills/tracking-quality/` | **技能 2 埋点质量** | ✅ 已验收 |
| `.dsh/skills/dashboard-interpret/` | 技能 4 看板解读 | ✅ 有效 |
| `.dsh/skills/`（另 10 个） | **社媒侧技能**（另一个项目合并进来的资产） | 本次未纳入验收 |
| `北极星实现/scripts/preflight-deploy-v2.mjs` | **部署门禁**（客观判据） | 🔴 当前阻断 6 项 |
| `北极星实现/glue/tools.json` | 3 个 Workflow 工具（ad-sync / attribution-query / token-audit） | 有效 |
| `北极星产品/app/key-routes-v3.cjs` | **key 保存前校验**（格式 + live 探活） | ✅ 已实现 + 独立验收（server-v5.cjs 接线） |
| `北极星产品/app/logging.cjs` | **结构化 JSON 日志**（三层零泄露防线） | ✅ 已实现 + 独立验收 |
| `北极星产品/app/server-v4.cjs` | 业务壳 + 日志接线 + `/healthz` 探活 | ✅ 已实现 + 独立验收 |
| `北极星产品/app/server-v5.cjs` | 业务壳 + key 校验接线 | ✅ 已实现 + 复核通过 |
| `北极星产品/app/server-v6.cjs` | 业务壳 + ratelimit-v2 + 看板租户透传（默认关） | ✅ 已实现 + 复核通过 |
| `北极星产品/app/ratelimit-v2.cjs` | **可信反代 IP**（`TRUST_PROXY` 开关 + 信任边界） | ✅ 已实现 + 复核通过 |
| `北极星产品/app/auth-routes-v4.cjs` | 登录/注册/防爆破改用 v2 的 `clientIp` | ✅ 已实现 + 复核通过 |
| `北极星产品/app/backup.cjs` / `restore.cjs` | **冷备份 / 冷还原**（rename 探测拒热拷贝） | ✅ 已实现 + 独立验收 |
| `北极星产品/app/key-migrate.cjs` / `key-import.cjs` | **master key 迁移**（PBKDF2 + AES-GCM） | ✅ 已实现 + 独立验收 |
| `北极星产品/Dockerfile` / `ecosystem.config.js` / `nginx.conf.example` | **部署物**（容器 / PM2 / 反代模板） | ✅ 已产出（域名/证书待用户填） |
| `北极星实现/交付版/start-x-v4.cjs` + `.dist-x-v4/` + `lib/deps-x-v4.cjs` | **看板后端 v4**（去演示回退，未知租户返回 null） | ✅ 已实现 + 复核通过 |

## 2.2 文档成果（当前有效 · 最新版）

### ★ 必读

| 文档 | 作用 | 最后更新 |
|---|---|---|
| **`北极星总文档-v1.8.md`**（本文档） | **唯一事实来源**：时间线 + 成果线 + 进度线 + 全量信息 | 2026-09-16 |

### 专题文档

| 文档 | 作用 | 最后更新 |
|---|---|---|
| `4.1-部署落地说明.md` | ★ MCP 接线怎么真正装上去（含"DSH 重写配置会不会冲掉接线"的正面回答、3 步自检、6 条坑） | 09-14 |
| `4.1-独立验收问题与返工记录-v1.md` | 4.1 验收问题闭环 + 4 条方法论 | 09-14 |
| `4.1独立验收报告-r7t9qx.md` | 4.1 独立验收报告（结论：**通过**） | 09-14 |
| `单元4.1-基座MCP接线-任务书与验收单元.md` | 4.1 任务书（含全部侦察结论） | 09-14 |
| `DSH基座-部署与隔离决策-形态B.md` | 产品形态 + **三条隔离铁律** + 证据 | 09-14 |
| `北极星实现/4.1-基座MCP接线-实施报告.md` | 4.1 实施报告（含 10 条验证原始证据） | 09-14 |
| `技能2独立验收报告-v8x3n.md` | 技能 2 首验（1 高 / 3 中 / 2 低） | 09-14 |
| `技能2复验报告-7tq3xk.md` | 技能 2 复验（**6/6 真修好，无回归**） | 09-14 |
| `技能2-独立验收问题与返工记录-v1.md` | 技能 2 返工闭环 + 4 条方法论 | 09-14 |
| `单元X-补齐X平台支持-任务书与验收单元.md` | X 平台任务书 | 09-14 |
| `单元X-代码补丁说明-逐行对照.md` | X 平台**逐行补丁**（4 处 + 验证件） | 09-14 |
| **`单元X独立验收报告-a7f3kq.md`** | ★ **X 平台独立验收报告（结论：通过 + 3 处非阻塞瑕疵）** | **09-14** |
| `北极星实现/ai/⚠️MCP配置-废弃说明与部署检查清单.md` | 废弃配置的说明（**注意：应删文件而非只写说明**） | 09-10 |
| `北极星实现/frontend/tabs/FIX-3.2-排序与空值.md` | 排序缺陷根因 + 修法 + 证据 | 09-11 |
| `FIX-空值口径专项.md` | 3 处空值当 0 的专项修复 | 09-11 |
| `北极星交付版-v1.45-独立验收问题与返工记录.md` | 交付版层面验收 + 返工 | 09-11 |
| `北极星交付版-v1.44进度与缺陷报告.md` | 交付版缺陷记录 | 09-11 |
| `北极星实现阶段-总收尾报告.md` | 实现阶段收尾 | 09-11 |
| `北极星范围-本期-v1.2.md` | **本期范围界定**（三层架构、DSH 基座 5 项） | 09-09 |
| `北极星产品/app/P1key校验-实现报告.md` / `P1key校验-落地说明.md` | P1 #7 key 校验实现 + 落地（39/39 自测） | 09-16 |
| `北极星产品/P1运维部署-实现报告.md` / `P1运维部署-落地说明.md` | P1 #10/#11/#13 运维-部署观测（31/31 自测） | 09-16 |
| `北极星产品/P1数据安全-实现报告.md` / `P1数据安全-落地说明.md` | P1 #12/#14 备份恢复 + key 迁移（26/26 自测） | 09-16 |
| `北极星产品/P1收尾-实现报告.md` / `P1收尾-落地说明.md` | P1-1 反代 IP + #6 看板隔离（初版） | 09-16 |
| `北极星产品/P1收尾-实现报告-v2.md` / `P1收尾-落地说明-v2.md` | #6 跨租户泄漏修复 + 回滚命令 + 隔离自测 | 09-16 |

### 设计文档（`北极星开发/`）

| 文档 | 内容 |
|---|---|
| `1-数据/` `2-广告/` `3-看板/` | 数据接入 / 广告归因 / 看板设计 |
| `4-DSH-AI/4.1-Ryze-MCP配置.md` | ★ **MCP 配置设计**（三道闸、工具三表、凭证边界、审计 15 字段） |
| `4-DSH-AI/4.2-Skill1打点推荐-Skill2埋点分析.md` | 技能 1/2 设计 |
| `5-胶水/` | 能力契约、tool 封装、胶水收尾 |

## 2.3 验证件（可复跑的检查）

| 检查件 | 作用 | 怎么跑 |
|---|---|---|
| `北极星实现/scripts/preflight-deploy-v2.mjs` | **部署总门禁** | `node 北极星实现\scripts\preflight-deploy-v2.mjs` |
| `北极星实现/交付版/selftest.cjs` | 交付版自检（116 断言） | `node 北极星实现\交付版\selftest.cjs` |
| `北极星实现/交付版/start.cjs` | 交付版启动（含 11 项安全自检 + 8 项 HTTP 自检） | `node start.cjs --port 8095` |
| `.dsh/skills/tracking-quality/test/smoke.py` | 技能 2 冒烟（84 断言） | `python .dsh\skills\tracking-quality\test\smoke.py` |
| `北极星实现/backend/verify-proxy.cjs` | 代理层验证（40 断言） | `node 北极星实现\backend\verify-proxy.cjs` |
| `北极星实现/交付版/_verify-sql-x.cjs` | X 迁移的 SQL 层验证 | `node ...` |
| `北极星实现/交付版/_verify-regression-map.cjs` | 回归映射验证 | ⚠️ **脚本自身有 bug**（见第三层瑕疵 1） |
| `北极星产品/app/自测-P1key校验.cjs` | P1 #7 key 校验自测（39 断言） | `node 北极星产品\app\自测-P1key校验.cjs` |
| `北极星产品/app/自测-P1运维部署.cjs` | P1 #10/#11/#13 自测（31 断言） | `node 北极星产品\app\自测-P1运维部署.cjs` |
| `北极星产品/app/自测-P1数据安全.cjs` | P1 #12/#14 自测（26 断言） | `node 北极星产品\app\自测-P1数据安全.cjs` |
| `北极星产品/app/自测-P1收尾.cjs` | P1-1 + #6 自测（31 断言） | `node 北极星产品\app\自测-P1收尾.cjs` |
| `北极星产品/app/自测-P1收尾-v2.cjs` | #6 泄漏修复自测（14 断言，真实 spawn + 真 HTTP） | `node 北极星产品\app\自测-P1收尾-v2.cjs` |

## 2.4 ⚠️ 已过期文档（**不要再当现状读**）

| 文档族 | 数量 | 为什么过期 |
|---|---|---|
| `北极星开发任务书与进度-v1.0.md` … `北极星开发任务书与进度-v1.22.md` | 23 | 设计阶段的历史快照 |
| `北极星实现任务书与进度-v1.0.md` … `北极星实现任务书与进度-v1.44.md` | 45 | **v1.44 停更于 09-11**，且 §2 单元清单全写"待开始"、§4 进度表全写"✅通过" —— **自相矛盾** |
| **`北极星总进度-v2.0.md`** | 1 | **已被本文档取代**（它只有"进度"一层；本文档按 PM 要求升级为三层 + 全量模块） |
| 北极星总文档-v1.0.md | 1 | 已被 v1.1 取代；v1.0 有自我引用错误 |
| 北极星总文档-v1.1.md | 1 | 已被 v1.2 取代（新增：多租户方案、问题与缺口清单、4 个新开发任务） |
| 北极星总文档-v1.2.md | 1 | 已被 v1.3 取代（批 1 门禁 v7 + X 源码化完成） |
| 北极星总文档-v1.3.md | 1 | 已被 v1.4 取代（批 2 归因 4 模块 + 批 3 多租户 U1~U5 全部收口） |
| 北极星总文档-v1.4.md | 1 | 已被 v1.5 取代（新增 §3.3b 生产发布缺口清单） |
| 北极星总文档-v1.5.md | 1 | 已被 v1.6 取代（P0 基础页面补齐 + 验收通过） |
| 北极星总文档-v1.6.md | 1 | 已被 v1.7 取代（P1 服务端硬化完成 + 验收通过） |
| 北极星总文档-v1.7.md | 1 | **已被本文档（v1.8）取代**（P1 收尾批次完成 + 验收通过） |
| `DSH基座-总进度看板-v1.0.md`、`DSH基座-总进度看板-v1.1.md` | 2 | 已被本文档取代 |
| `DSH基座-技能接真数据-任务书与进度-v1.0.md` | 1 | 已被本文档取代 |
| `DSH基座-技能接真数据-补充发现-E与对账教训.md` | 1 | 其结论已收入本文档（铁律 #6） |
| `北极星实现/ai/mcp-ryze-config.json` + `北极星实现/ai/mcp-ryze-config.说明.md` | 2 | 🔴 **危险废弃文件，必须删**（见 §4.8） |

> **为什么会有这么多版本**：开发期沙箱对**已存在文件完全只读**（实测：命令结束后文件永久只读，跨命令改不了也删不了）。
> 每次修订只能「新建下一版」。**这是环境约束下的权宜做法，不是设计意图。**

---

# 第三层 · 进度线

> **这一层回答："做完什么了？正在做什么？还要做什么？"**

## 3.1 已完成 ✅

### 数据与看板层（20 个单元）

| 单元 | 内容 | 验收 | 返工次数 | 备注 |
|---|---|---|---|---|
| 1.1 | 数据库表结构 + 迁移 | ✅ 通过 | 0 | 4/4 |
| 1.2 | Webhook 接收服务 | ✅ 通过 | 0 | 4/4 |
| 1.3 | 幂等落库 | ✅ 通过 | 0 | 5/5 |
| 1.4 | 事件查询 API | ✅ 通过 | **6** | 6 次验收/返工 |
| 1.5 | Token 审计 | ✅ 通过 | 0 | 4/4 |
| 2.1 | Ryze MCP 对接 | ✅ 通过 | 0 | 4/4 + 4 处加固（含高危后门） |
| 2.2 | 广告数据清洗落库 | ✅ 通过 | 0 | 4/4，中危整改已完成 |
| 2.3 | JOIN 归因 | ✅ 通过 | 0 | 4/4 + 1 处补漏 |
| 2.4 | Funnel API | ✅ 通过 | **5** | 三个原坑全规避 |
| 3.1 | 看板骨架 | ✅ 有条件通过 | 1 | **5 条部署门禁** |
| 3.2 | 总览 + 素材 Tab | ✅ 通过 | 1 | 返工后经独立复验（vm 实测 + LCS 逐行比对） |
| 3.3 | 用户 + 产品 Tab | ✅ 通过 | 0 | 4/4，1 处 P3 |
| 3.4 | 健康 Tab | ✅ 通过 | 0 | 3/3，1 处 P3 |
| **4.1** | **Ryze MCP 配置** | ✅ **通过** | **3** | 09-14 重新接线 + 独立验收（红线三条独立复现） |
| **4.3** | **Skill2 埋点质量** | ✅ **通过** | 1 | 实现为 `tracking-quality`，84 断言 + 复验 6/6 |
| 5.1 | 能力契约落地 | ✅ 通过 | 0 | 4/4 |
| 5.2 | Tool 封装 | ✅ 通过 | 1 | 4/4 |
| 5.3 | 胶水收尾 | ✅ 通过 | 0 | 5/5；自检盲区已由 v2 修复 |

**里程碑**：数据接入层 1.x 全部完成（5/5）

### ★ 单元 X · 补齐 X 平台支持（**09-14 独立验收通过**）

| 项 | 状态 | 证据 |
|---|---|---|
| 问题定性 | ✅ | 前端有 X、后端不认 —— **后端实现缺口**（X 占 ~15.5% 流量） |
| SQL 迁移 `003_add_x_platform.sql` | ✅ 已产出、可跑、幂等 | 空库 001+003：`x` 五表全插成功、`bogus` 五表全拒、重跑不报错 |
| 代码补丁（**4 处**） | ✅ 已产出逐行说明 | `ryze-client.ts` / `funnel.ts` / `attribution.ts`×2 |
| **API 层端到端** | ✅ **`platform=x` → HTTP 200** | 2 组 / 750 访问 / 182 注册 / 12 充值 / **$1,188.00** |
| 两条 X 边界场景 | ✅ 原样出现 | `img_brand_awareness`/JP = 450 访问 **0 充值**；`orphan_creative_zz`/SG = 300 访问 **12 充值** |
| meta/google 零回归 | ✅ | meta 200（3 组 2610 访问）、google 200（2 组 1470 访问）、**`bogus` 仍 400** |
| **独立验收** | ✅ **通过** | `单元X独立验收报告-a7f3kq.md` |

**★ 这次验收最有价值的一件事**：验收方**自己造了一个"只改前 3 处"的反例版本**，实测得到
`platform=x` → **200 但分组 = 0** —— 证明了实现者"任务书漏了第 4 处"的说法成立。
（只改 3 处 = **看起来成功、其实没数据**的假成功。）

**3 处非阻塞瑕疵（如实记录）**：

| # | 瑕疵 | 严重性 | 说明 |
|---|---|---|---|
| 1 | `_verify-regression-map.cjs` **脚本自身有 bug** | 🟡 中 | 打印"meta 分支逐字一致: false"，因 `split("WHEN ")` 把 `ELSE NULL END` 尾巴黏到最后分支。**结论本身正确，但工具是坏的** |
| 2 | 文档 §3 的 JSDoc 注释改动在 `.dist-x` 里没落地 | ⚪ 低 | 纯注释，零影响 |
| 3 | X 边界场景**只验到行为层**；`roi_by_entity` 在 `platform=x` 下为 **0 行** | 🟡 中 | 种子把 X 素材花费记在 google 下、orphan 归 `platform=null` 被过滤 → **ROI 层的"有花费无充值/未命中"语义未真正在 x 上验证** |

### DSH 基座

| 项 | 状态 |
|---|---|
| 对话 Agent | 🟢 复用 DSH 原生 |
| 工具调用链路 | 🟢 复用 DSH 原生 |
| **Ryze MCP client** | 🟢 **已接线 + 独立验收通过** |
| 新老用户路由 | 🟢 复用 DSH 原生 |
| **技能 4 看板解读**（`dashboard-interpret`） | 🟢 已完成 |

### 交付版

| 项 | 状态 |
|---|---|
| 可运行交付版（8095） | 🟢 在跑 |
| 安全自检 11 项 | 🟢 全过 |
| HTTP 自检 8 项（含 3 项负向） | 🟢 全过 |
| 5 个 Tab 浏览器逐页验收 | 🟢 通过 |
| 自检 116 断言 | 🟢 全过 |

### 产品层（业务壳，形态 B）

| 项 | 状态 | 证据 |
|---|---|---|
| 业务壳端到端验收 | 🟢 **25/25 通过** | `北极星产品/app/_验收-业务壳.cjs` |
| 看板代理 | 🟢 已打通 | `/api/analytics/funnel` → 200，4830 访问，平台 x/meta/google |
| 技能隔离 | 🟢 产品 3 技能，社媒 10 技能 **0 泄漏** | `北极星产品/运行时/workspace/.dsh/skills/` |
| 技能 6 策略建议 | 🟢 **独立验收通过** | `单元6独立验收报告-k9x4m2.md` |
| 全景图 | 🟢 已生成 | `北极星全景图.html` |

## 3.2 进行中 🟡

| 项 | 状态 | 说明 |
|---|---|---|
| 总文档 v1.2 更新 | 🟡 进行中 | 收编问题清单 + 多租户方案 + 4 个新任务 |
| 开发（门禁 v4 / X 源码化 / 归因 4 模块 / 多租户） | ⚪ 待指派 | 按 §3.3 顺序，实现者 ≠ 验收者 |

## 3.3 待做 ⚪

> **开发顺序已由 PM 拍板（2026-09-15）**：① 门禁 ✅ ② X 源码化 ✅ ③ 归因 4 模块 ✅ ④ 多租户 U1~U5 ✅ —— **三批全部收口**。详见《问题与缺口总清单 v1.0》第六节。

| 优先级 | 事项 | 依赖 | 说明 |
|---|---|---|---|
| ✅ 已完成 | 门禁 v7（四轮迭代 v4→v5→v6→v7，独立验收通过） | — | `preflight-deploy-v7.mjs`，剩余为文档化边界 |
| ✅ 已完成 | X 源码化（6 处补丁落源码，独立验收通过） | — | 交付物 `src-x`/`db-x`/`lib-x`，落地见 `X源码化-落地说明.md` |
| ✅ 已完成 | **归因面板 4 模块**：调用渠道 / 全局模型明细 / 国家漏斗对比 / 国家下钻 | — | 调用渠道、模型明细接真实后端 `by_source`/`by_model`；广告侧无数据就空处理；落地见 `归因4模块/归因4模块-落地说明.md` |
| ✅ 已完成 | **多租户 U1~U5**：身份库 + key 加密 + 每租户 workspace + 前端 + 收口 | — | U1~U5 五单元独立验收全过（含提权/假 stub/死代码三处返工）；落地见 `北极星产品/多租户U5-落地说明-v2.md` |
| P2 | 技能 1 打点推荐 | 无 | 部分可做（有静态配置 + 契约） |
| P2 | 技能 3 对话 | 基座已通 | |
| P2 | 4.1 剩余 3 项工程 | 不依赖 Ryze | OAuth / Token 加密 / 续期断开 |
| ✅ 已闭环 | **PT key 充值后真实 LLM 验证**（09-20，新 key 尾号 `IQi2`） | 充值完成 | 多轮续接/逐字流式/进程复用/轨迹可见/停止/崩溃恢复全过；修复 3 缺陷（`6706bd8`/`bf53b69`） |
| P2（非 bug） | **常驻 agent 迁移 v1 限制** | — | 单租户进程顺序队列（同时只一轮）；切模型对新对话生效、老对话 session 仍是老模型（设计限制，保留） |
| P3 | 修 `_verify-regression-map.cjs` 的 bug | 无 | 工具坏了会误判 |
| P3 | 技能 5 解析 | 依赖真 Ryze | |
| P3 | 接真 Ryze | 外部 | **已定：暂缓** |
| P3 | 4.1 剩余：上游真实审计、越权告警 | 依赖真 Ryze | |
| P3 | 社媒 10 技能接入产品 | 多租户完成后 | 现产品只有 3 个北极星技能 |
| P3 | 清理历史版本快照 + 临时文件 | 需写权限 | 见 §4.9 |

> **产品界面「X-Ads」筛选：已澄清为"保留"** —— 要补后端，不是删前端。

## 3.3b 🔴 生产发布缺口清单（2026-09-15 自查 · 距离发布还差什么）

> 定位：本文档记录「离发布生产环境还缺什么」。当前已完成的是**后端骨架 + 基础页面**，
> 但**用户能直接用的页面**和**运维/生产化**还差不少——不是 demo 级收尾，是发布级补齐。

> ✅ **v1.8 状态更新（2026-09-16）**：P1 #5/#7/#8/#9/#10/#11/#12/#13/#14 已全部标「✅ 已补齐」；
> #6 已标「✅ 已实现（A 方案默认关，数据侧待接入）」；#10 标「✅ 配置模板已出（域名/证书待用户填）」；
> 新增 P1-1「反代后 IP 塌缩」标「✅ 已修」；P2 #15-18 仍「待做」。本批 P1 全部经**独立验收**（实现者 ≠ 验收者）。

### 🔴 诚实边界 / 待用户提供（醒目提示 · v1.8 新增）

> 以下 4 条是**当前仍未解决、需要用户 / 产品负责人提供输入或亲自执行**的事项，如实记录、不掩盖：

| # | 事项 | 需要谁做什么 |
|---|---|---|
| 1 | **#6 数据侧待接入** | 每租户看自己数据的数据前提不满足——看板库只有 **1 个演示租户**；非演示租户在 A 方案下看到空数据是 **RLS 正确结果、非造假**。需按业务壳租户 UUID 灌入 `pt_events`/`ad_*`/`tenant_workspaces` 数据 |
| 2 | **#10 需正式域名 + 证书路径** | 已出 nginx 模板（TLS 终止 + 80→443 跳转），但 `<你的域名>`、`<证书路径>` 是占位，需用户填真实值 |
| 3 | **#16 需 PowerTokens 门户确切网址** | 现在占位 `https://powertokens.ai`，需用户提供真实门户地址 |
| 4 | **沙箱冻结** | 所有落地动作（`Copy-Item` 覆盖 `server.cjs` 等）需**用户本机执行**——本 agent 改不了已存在文件（沙箱对已存在文件只读） |

### P0 —— 发布前必须补（基础用户页面）✅ 已补齐（实现 + 独立验收通过，待落地生效）
| # | 缺口 | 现状 | 要补什么 |
|---|---|---|---|
| 1 | **注册页** | `login.html` 只有登录，无注册入口（注册仅 `/api/auth/register` 接口） | 注册页 + 登录页加「注册」链接 + 注册后引导 |
| 2 | **设置/key 入口** | `index.html` 无「设置」按钮，只能手敲 `/settings` | 主界面加「我的 Key」导航 + 未绑 key 时显眼提示 |
| 3 | **没绑 key 的前端提示** | `/api/chat` 返回 409 needKey，但 `app.js` 不识别，显示「执行失败」 | 前端捕获 needKey → 引导去设置页绑 key |
| 4 | **注册后落点** | 注册成功无引导流程 | 注册完自动进设置页绑 key |

> ✅ **P0 四项已全部实现，独立验收通过（2026-09-15，29/29 自测 + 端到端）**：
> ① 注册（登录页 tab + 确认密码 + 自动登录跳 /settings）② 「我的 Key」入口 + 未绑红点/提示条
> ③ needKey 友好提示（「去绑定」）④ 注册后引导。落地 = 3 行 Copy-Item（`北极星产品\P0页面-落地说明.md`），
> 落地前须先跑多租户后端 `server-v2.cjs`（旧 `server.cjs` 尚未被 U5 替换，见 `P0页面-落地说明-修正.md`）。

### P1 —— 发布前应该有（核心正确性）
| # | 缺口 | 现状 | 要补什么 |
|---|---|---|---|
| 5 ✅ | **会话持久化** | conversations 表落库，按 user_id 隔离，重启不丢 | 已补齐（server-v3.cjs + 003_conversations.sql） |
| 6 ✅ | **看板多租户隔离** | A 方案已实现：业务壳 `PT_DASH_TENANT_INJECT` + 看板后端 `TRUST_TENANT_HEADER` + 去演示回退（未知租户返回 null，不再泄漏演示工作区） | 已实现，**默认关**；**数据侧待接入**（每租户自己数据的前提不满足——看板库只有 1 个演示租户，非演示租户 A 方案下看到空数据是 RLS 正确结果、非造假） |
| 7 ✅ | **key 有效性校验** | 保存 key 先验真伪再落库（本地格式校验 + live 探活 `GET /v1/models` + Bearer，**真 key→200/假 key→500 已实测**，非 2xx fail-closed 拒绝并给中文提示） | 已补齐（key-routes-v3.cjs，server-v5.cjs 接线） |
| 8 ✅ | **登录暴力破解防护** | 用户名+IP 滑动窗口 5次/10分钟锁10分钟，中文提示 | 已补齐（ratelimit.cjs + auth-routes-v3.cjs） |
| 9 ✅ | **API 限流** | login 10/分、register 5/分、chat 20/分，429 中文提示 | 已补齐（ratelimit.cjs） |

### P1 —— 运维 / 生产化
| # | 缺口 | 现状 | 要补什么 |
|---|---|---|---|
| 10 ✅ | **HTTPS / 域名** | nginx 反代模板已出（TLS 终止 + 80→443 301 跳转 + `/healthz`），域名/证书为占位 | **配置模板已出，域名/证书待用户填**（`nginx.conf.example`） |
| **P1-1 ✅** | **反代后客户端 IP 塌缩**（运维验收抓出，并入收尾） | nginx 反代后 remoteAddress 全是 127.0.0.1 → chat 限流退化成全站 20/分、防爆破 IP 维度连坐锁整站 | **已修**：`ratelimit-v2.cjs` 加 `TRUST_PROXY` 开关 + 信任边界（校验合法 v4/v6 才采信，默认关防伪造；配套 `auth-routes-v4.cjs`） |
| 11 ✅ | **部署方式** | Dockerfile + PM2（ecosystem.config.js）+ 部署说明（NSSM/WinSW / systemd / PM2 / Docker 四套） | 已补齐 |
| 12 ✅ | **数据库备份/恢复** | backup.cjs 冷备份 + restore.cjs 冷还原（rename 探测拒热拷贝 + 还原后 SELECT 1 校验） | 已补齐 |
| 13 ✅ | **日志/监控/告警** | logging.cjs 结构化 JSON 日志（三层零泄露防线）+ `/healthz` 探活 | 已补齐 |
| 14 ✅ | **master key 迁移** | key-migrate.cjs（PBKDF2-SHA256 + AES-256-GCM 导出）+ key-import.cjs（目标机 DPAPI 重落盘） | 已补齐 |

### P2 —— 增强（仍待做）
| # | 缺口 | 要补什么 |
|---|---|---|
| 15 | 忘记密码 / 改密码 | 邮件 / 自助重置（**仍待做**） |
| 16 | PT 门户确切地址 | 现在占位 `https://powertokens.ai`（**仍待做**，需用户提供确切网址） |
| 17 | 移动端响应式 | 登录 / 设置 / 对话适配手机（**仍待做**） |
| 18 | 密码强度校验 | 注册时校验复杂度（**仍待做**） |

### P2 验收发现的非阻塞项（v1.8 汇总 · 均不影响本批发布）

> 以下是本批 4 个 P1 单元**独立验收过程中发现**、但判定为 P2（非阻塞）的遗留项，如实登记、留待后续：

| 来源单元 | P2 项 | 说明 |
|---|---|---|
| #12/#14 数据安全 | restore 校验失败应返回非 0 | 当前 exit 0 会误导「还原成功」 |
| #12/#14 数据安全 | restore 缺 `--backups-dir` 旗标 | 备选备份目录入口未暴露 |
| #10/#11/#13 运维部署 | `.gitkeep` 路径表述偏差 | 文档小瑕疵 |
| #10/#11/#13 运维部署 | 落地说明「前置条件」与仓库现状不符 | 文档待对齐 |
| #10/#11/#13 运维部署 | 字段名黑名单精确匹配覆盖不足 | 日志脱敏黑名单为精确匹配，需防变体 |
| #10/#11/#13 运维部署 | `/healthz` 探活日志噪声 | 探活请求也进访问日志，需降噪 |
| #10/#11/#13 运维部署 | 自测 C6 断言偏弱 | 可加强 |
| #7 key 校验 | 429 限流也 fail-closed 的措辞 | 应再区分「暂时失败 / 确定无效」 |
| #7 key 校验 | 自测 B4 断言覆盖不全 | 可加强 |
| #6 看板隔离 | 实现报告一处计数标签笔误 | 文档小瑕疵 |
| #6 看板隔离 | start-x-v4 启动横幅打印演示 webhook secret | 既有演示占位值、非真实密钥，仍应去掉 |

### 已完成的（避免误判，这些是真做完了）
身份库（注册/登录/登出/会话，scrypt+HttpOnly）✅、key 加密（DPAPI+AES-GCM）✅、
租户隔离（每租户 workspace + BYOK 注入）✅、设置页（key 配置）✅、
归因看板基础 + 4 模块 ✅、部署门禁 + X 源码化 ✅、
P0 基础页面（注册/Key入口/needKey）✅、P1 服务端硬化（会话持久化/防爆破/限流）✅、
**P1 收尾批次**（key 校验 / 运维-部署观测 / 数据安全 / 看板隔离 + 反代 IP 修复）✅。
## 3.4 ✅ 部署门禁状态（已通过）

**当前脚本 `preflight-deploy-v7.mjs` → 7 通过 + 4 提醒（EXIT=0），无失败。经独立验收通过（剩余为文档化边界）。**

- 发布前跑：`node 北极星实现\scripts\preflight-deploy-v7.mjs`
- 四轮迭代：v4（15 漏洞）→ v5（过校正）→ v6（残留）→ v7（通过）；验收方每轮独立证伪，最终只剩「已文档化边界」。
- 文档化边界（脚本末尾 §边界 已逐条写明）：字段名名单、直连域名清单需同步维护、document 别名/变量间接、换变量名、完全动态加载等。

---

# 第四层 · 全量信息模块

## 4.1 项目是什么

**北极星（PowerTokens）** —— Token 业态的**数据闭环**产品。

**核心链路**：
```
用户广告账号(Meta/Google/X) → Ryze(第三方连接器) → 我方只读代理 → 归因引擎 → 看板/API
                                                      ↑
                                               白名单在这收口
                                       用户行为事件(Webhook) → 事件库 ──┘
```

**三层架构**：①载体 DSH 基座 ②业务壳 ③业务线 ｜ **胶水** = adapter
（跨平台适配**只在业务资产层**做，不改基座）

## 4.2 技术栈（已拍板）

| 层 | 选型 |
|---|---|
| 后端 | **Node.js + TypeScript** |
| 前端 | **原生 JS + HTML（无构建步骤）** |
| 数据库 | **PostgreSQL**（开发/演示用 **PGlite** = WASM 版真 PG，纯 JS 无需安装） |

**6 层架构**：①AI Agent(DSH) ②确定性 Workflow ③数据接入(Webhook+MQ) ④存储 ⑤第三方集成 ⑥前端 UI

## 4.3 数据模型要点

**事件白名单（6 个，`backend/src/collect/validate.ts:24`）**：
`visit` / `signup` / `key_created` / `model_call` / `recharge` / `auto_recharge_toggle`

**复合主键**：`(tenant_id, event_id)`

**★ 三套互不相通的事件名词表**（重要，容易误判）：
| 侧 | 事件 |
|---|---|
| PT 业务侧 | 上述 6 个 |
| SS-GTM 审计侧 | `token_consume` / `credit_purchase` / `balance_recharge`（3 个） |
| 契约纸面 | `api_call` / `usage_exceed`（2 个）—— **后端任何地方都不落** |

**租户隔离**：`BEGIN READ ONLY` + `set_config('app.current_tenant_id', $1, true)` + 显式 `WHERE tenant_id = $1::uuid`；
应用以**非超级用户 NOBYPASSRLS** 角色 `pt_app` 连接。

**广告表平台约束**：`CHECK (platform IN ('meta','google'))`（5 张表：`ad_accounts` / `ad_campaigns` / `ad_adgroups` / `ad_creatives` / `ad_performance_daily`）
→ **X 迁移 `003_add_x_platform.sql` 待应用**

## 4.4 API 契约要点

**⚠️ 响应信封**：funnel 返回是**顶层的 `{from,to,granularity,platform,country,groups}`，没有 `data` 包装**。
（读 `json.data.groups` 会得到 undefined，**看起来像"没数据"** —— 已踩过两次：我 + X 验收方）

**⚠️ 时间参数**：`from`/`to` 是**毫秒时间戳**，不是 ISO 日期串。

**主要端点**：
| 端点 | 说明 |
|---|---|
| `GET /api/analytics/funnel` | 漏斗聚合（`granularity=total\|day`，`platform`，`country`，`group_by`） |
| `GET /api/analytics/events` | 事件查询（`items,next_cursor,has_more,count`；**`limit` 静默钳制在 500**） |
| `GET /api/analytics/audit/token` | Token 最小审计报告（6 字段报告） |

**鉴权**：只读 token 前缀 `pt_ro_`，交付版演示 token = `pt_ro_delivery_9f3c21`

## 4.5 已知缺陷（跨模块）

| # | 问题 | 状态 |
|---|---|---|
| 1 | 三套互不相通的事件名词表 | ⚪ 未修 |
| 2 | `auto_recharge_toggle` **从未被埋过** | ⚪ 未修（覆盖率永远 5/6） |
| 3 | 事件接口 `limit` **静默钳制在 500** | ⚪ 未修 |
| 4 | 产品 Tab 的"Token 消耗暂未接入"**是误导** | ⚪ 未修（数据其实全在：79,600 条 `model_call` 都带 token 字段，是**接口没暴露**） |
| 5 | 交付版数字随**滚动窗口**变小 | ✅ 已在 README 加提醒 |
| 6 | X 平台后端不支持 | 🟡 **运行版已支持**（8095 实测 200），🔴 **源码未改、重编译退回** |
| 7 | 部署门禁 | 🔴 v3 9过2提醒0失败，但独立验收指出 4 子问题（见 §3.4），待「门禁 v4」 |
| 8 | `_verify-regression-map.cjs` 脚本自身有 bug | ⚪ 未修（工具坏了会误导以后判断） |
| 9 | X 的 ROI 层语义未真正验证 | ⚪ 待接真 Ryze |
| 10 | **多租户缺失 + 开发者 key 泄漏** | 🔴 产品 `.credentials.yaml` 与开发者逐字节相同；单账号；会话内存不落盘。**已定案待实现**（见 §4.13） |
| 11 | 门禁 v3 假阴性（[7] 剥注释吞字符串） | 🔴 待「门禁 v4」修复 |
| 12 | 门禁 [7] 查未加载文件 | 🔴 同上 |
| 13 | X 源码未改（运行版能用，重编译退回） | 🔴 待「X 源码化」 |
| 14 | 对话 Markdown 不渲染（用 textContent） | 🟡 影响答案可读性 |
| 15 | 对话延迟 ~262s（无流式/缓存） | 🟡 影响体验 |
| 16 | 社媒 10 技能未接入产品 | 🟡 产品只有 3 个北极星技能 |

## 4.6 ★ 铁律（14 条，每条都是踩坑换来的）

| # | 铁律 | 来源 |
|---|---|---|
| 1 | **实现者 ≠ 验收者**，每单元独立验收，通过才解锁下游 | 项目治理 |
| 2 | **"验文本" ≠ "验行为"** | 曾发现"前端断言只检查字符串存在" |
| 3 | **一个永远不会失败的检查，比没有检查更糟** | 多个空转检查；X 验收又遇到"会误报失败的检查" |
| 4 | **断言必须从"用户真正走的入口"驱动** | 技能2 鉴权高危问题的根因 |
| 5 | **SKILL.md 里每句"我能做 X"，都要能指到实现代码 + 验证断言** | 多次过度声称 |
| 6 | **滚动时间窗系统里，对账前必须先固定 `from`/`to`** | 差点误报的"跨组件不一致" |
| 7 | **鉴权失败 ≠ 没有数据** | 技能2 高危问题 |
| 8 | **证据不足时不许给分/给结论** | 60% 权重未知仍亮绿灯 |
| 9 | ★ **任何「某件事没有发生」的结论，必须先证明检测机制是活的** | 4.1 报警器验证 |
| 10 | ★ **实现者的"自我辩解"必须交给独立方裁定，哪怕它是诚实的自曝** | 4.1「等价替代」裁定；X 的"第 4 处改动"也是这么裁定的 |
| 11 | ★ **验收不只查"对不对"，还要查"会不会被读错"** | 4.1 数字口径问题 |
| 12 | ★ **发现前后端/组件间不一致时，不许默认"多出来的那部分"是错的 —— 先问需求** | X 平台筛选项（AI 差点误删） |
| 13 | ★ **报告"某处失败"之前，先确认这个判断的适用范围（哪个目录、哪个版本）** | 门禁 [5][7] 误读 |
| 14 | ★ **总文档每次发布前必须做「自洽性自查」**：① 自我引用是否指向本文件 ② 提到的每个文件名是否真实存在 ③ 被取代的文档是否都进了"已过期"清单 ④ 状态描述是否与进度线一致 | **PM 抓到总文档自我引用写错（v1.1 修正）** |

> **第 12、13 条是 AI 在同一天犯的两次同类错误** —— 都是**没先确认"判断的适用范围"就下结论**。
> **第 14 条来自 PM** —— **"唯一事实来源"的另一面是"唯一的错误来源"，所以更要自查。**

## 4.7 术语表

| 术语 | 大白话 |
|---|---|
| **DSH 基座** | AI 那层的地基：会对话、能调工具、能挂技能 |
| **MCP** | 一种"让 AI 连外部数据源"的标准接口，像 USB 口 |
| **Ryze** | 第三方连接器，帮我们连 Meta / Google / X 广告后台 |
| **只读代理** | 我们自己写的一道关卡：只许读、不许写，且只放行白名单里的工具 |
| **白名单 / 默认拒绝** | 名单上有的才放行；不在名单上的一律不可见、不可调 |
| **结构上不存在** | "注册表里根本没这个工具"，而不是"工具在但拒绝执行" |
| **红线** | 写工具（改预算、停投、删素材）必须"结构上不存在" |
| **独立验收** | 换一个没参与开发的人（agent）来挑毛病，开发者不能自证 |
| **端到端验证** | 从用户真正走的入口跑一遍，而不是只测某个小函数 |
| **替身上游** | 因真 Ryze 不存在，先造个假的来验"机制"对不对 |
| **尝试证伪** | 不是"信"也不是"疑"，而是**自己构造反例去试** |
| **部署门禁** | 上线前必须全绿的自动检查脚本 |
| **滚动窗口** | 数据窗口随时间往前滚，同一查询在不同时刻结果会变 |
| **归因** | 判断"这笔充值/这个用户是被哪个广告带来的" |
| **ROI** | 行为侧真实充值 USD ÷ 广告花费 |
| **cordis.patch.yml** | DSH 的插件装配文件（不是 `settings.yaml`） |

## 4.8 ★ 部署与隔离（重要）

### DSH 的真实装配机制（**纠正过的错误假设**）

**设计文档假设配置写在 `settings.yaml` 的 `mcp:` 段 —— 是错的。**

**真实机制**：`cordis.patch.yml` 分层叠加
```
dsh-base 的 patch
  → surface（如 dsh-web-app）的 patch
    → profile 自己的 cordis.patch.yml     ← ★ 接线写这里
      → --patch 命令行覆盖层
```

**关键结构**：
```
$DSH_HOME/profiles/headless/
├── cordis.yml          ← DSH 每次启动会重写它（恒为基础空配置 []）
├── cordis.patch.yml    ← ★ 我们的接线放这里（DSH 只读，不写）
└── package.json        ← 声明加载哪些 bundle
```

**⚠️ 正因为 `cordis.yml` 每次被重写，千万不要把接线写进 `cordis.yml`。**

### ★ 三条隔离铁律

| # | 铁律 |
|---|---|
| 1 | **绝不挂开发者的 `~/.dsh`** —— 用 `DSH_HOME` 环境变量指向产品自有的 home |
| 2 | **技能必须搬出开发工作区** —— 放工作区里会被开发动作连带改掉 |
| 3 | **DSH 端口只在容器内回环** —— 绝不开 `0.0.0.0` |

> DSH 有一条硬规则：**拒绝 `--host 0.0.0.0`**（原话"会把远程代码执行暴露到网络上"）—— **有意为之的安全设计，不要去绕开**。

### ★ 常驻 agent（SDK 模式）部署形态（2026-09-17 迁移 · 替代 headless 一次性 spawn）

**迁移前**：业务壳每次对话用 `dsh --profile headless` **一次性 spawn** 一个 DSH 进程，跑完即销毁 → 每次冷启动 ~6s。

**迁移后**：正式 Cordis 插件 `persistent-runner.mjs`，直接用 **DSH SDK 层程序化 API**（`agents.create` / `followup` / `whenIdle` / session/event 流）驱动，**每租户一个常驻 DSH 进程**（1 用户 = 1 租户，进程隔离 key 与 cwd）。不再是 patch node_modules 的 headless 一次性 spawn。

**核心机制**：
- **session 持久**：DSH session 持有对话轨迹，同一 `conversationId` 复用同一 session；业务壳在「热会话」时**不再重复传历史**，只在冷启动（首次 / 进程重启后）重放历史。
- **轨迹可见**：新增 `tool/call` + `tool/result` 事件透传到前端，可折叠「🛠️ 过程 · N 步」展示，并落库到消息 `steps` 字段。
- **逐字流式**：`assistant/chunk` 的 `text-delta` 继续 SSE 逐字下发，未被破坏。
- **每用户独立 session**：每租户进程 + 每对话 session，用户间天然隔离。

**实测证据**：连续两问，第 1 问 6338ms（含进程冷启动）→ 第 2 问 306ms（复用常驻进程），冷启动消除约 6 秒；流式、多轮累加正常。

### 🔴 危险文件（必须删）

`北极星实现/ai/mcp-ryze-config.json` 指向 **DSH 直连 Ryze**，而它自己的说明写着
「须由 DSH 侧包装过滤器在 `tools/list` 同步时裁剪」—— **那个过滤器不存在**。

**一旦误加载：Ryze 全部工具（含 9 个写工具）会被无条件注册给 AI** —— 就是 4.1 第 1 次验收被否掉的那个洞。

**救命的一条**：门禁 ③ 已堵住最坏后果 —— **DSH 环境不允许出现 `RYZE_MCP_TOKEN`**（检测到即阻断）。
所以即使误载，也只会 401 连接失败，**不会真的误改广告**。

**为什么当时没删**：处置方式是"加一份废弃说明文档"，**没删文件**（大概率也是沙箱改不动）。
**但门禁脚本不认文档，只认文件在不在。**

## 4.9 环境与命令

### 环境

| 项 | 值 |
|---|---|
| 工作区 | `C:\Users\admin\WorkBuddy\2026-08-17-13-57-38\dsh-workspace` |
| `DSH_HOME` | `C:\Users\admin\.dsh` |
| DSH Web GUI | `http://127.0.0.1:3080` |
| 交付版 | `http://127.0.0.1:8095/?pt_ro_token=pt_ro_delivery_9f3c21` |
| MCP 代理端口 | `127.0.0.1:3001` |
| Node | `C:\Users\admin\.workbuddy\binaries\node\versions\22.22.2` |
| `dsh` | **不在 PATH 上**（由 WorkBuddy Electron 启动）；找 `@deepseek-ai/dsh-cmdline` |

### ⚠️ 沙箱约束（影响工作方式）

**本会话沙箱：新建的文件，在创建它的命令结束后就永久只读 —— 跨命令改不了也删不了。已存在的文件同样不能改不能删。**

实测结果：
```
① 同一命令内新建的文件    → ✅ 能改
② 命令结束后（跨命令）    → ❌ 永久只读，既不能改也不能删
③ 已存在的文件            → ❌ 不能改、不能删
```

- 这就是为什么文档都是"新建下一版"
- 需要改动/删除的操作，**只能交给产品负责人执行**

### 操作命令（**需要写权限，请产品负责人执行**）

```powershell
cd C:\Users\admin\WorkBuddy\2026-08-17-13-57-38\dsh-workspace

# ① 🔴 删掉废弃的直连配置（安全项，建议尽快）
Remove-Item "北极星实现\ai\mcp-ryze-config.json" -Force
Remove-Item "北极星实现\ai\mcp-ryze-config.说明.md" -Force

# ② 代理版配置改成规范名（门禁 [2] 要求"只剩一个语义配置"）
Move-Item "北极星实现\ai\mcp-ryze-config.proxy.json" "北极星实现\ai\mcp-ryze-config.json"
Move-Item "北极星实现\ai\mcp-ryze-proxy.说明.md" "北极星实现\ai\mcp-ryze-config.说明.md"

# ③ 源目录前端收口（把修正版覆盖正版，删掉并存文件）
Copy-Item "北极星实现\frontend\app.fixed.js" "北极星实现\frontend\app.js" -Force
Remove-Item "北极星实现\frontend\app.fixed.js" -Force
Copy-Item "北极星实现\frontend\tabs\creatives.fixed.js" "北极星实现\frontend\tabs\creatives.js" -Force
Remove-Item "北极星实现\frontend\tabs\creatives.fixed.js" -Force

# ④ 历史版本快照收敛（每系列只留最新一版）
Remove-Item "北极星总进度-v2.0.md" -Force
Remove-Item "北极星总文档-v1.0.md" -Force
Get-ChildItem -File -Filter "北极星开发任务书与进度-v*.md" |
  Where-Object { $_.Name -ne "北极星开发任务书与进度-v1.22.md" } | Remove-Item -Force
Get-ChildItem -File -Filter "北极星实现任务书与进度-v*.md" |
  Where-Object { $_.Name -ne "北极星实现任务书与进度-v1.44.md" } | Remove-Item -Force

# ⑤ 重跑门禁验收（必须变绿）
node 北极星实现\scripts\preflight-deploy-v2.mjs
```

### ⑥ 🔴 X 补丁落地（**P0，做完 8095 才能查 X**）

**详细逐行补丁见 `单元X-代码补丁说明-逐行对照.md`。** 摘要：

```powershell
# 6.1 改 4 处源码（照补丁说明逐行改）
#     · 北极星实现/backend/src/ads/ryze-client.ts   —— Platform 类型加 "x"
#     · 北极星实现/backend/src/ads/funnel.ts        —— PLATFORMS 加 "x"
#     · 北极星实现/backend/src/ads/attribution.ts   —— DEFAULT_SOURCE_PLATFORM_RULES 加 x 规则
#     · 北极星实现/backend/src/ads/attribution.ts   —— buildSourcePlatformCase 泛化
#     · 北极星实现/backend/db/001_init.sql          —— 5 处 CHECK 加 'x'

# 6.2 重新编译
node "北极星实现\交付版\build.cjs"

# 6.3 ★ 把 003 加进交付版的迁移清单（否则新库不会有 X 约束）
#     见 北极星实现/交付版/lib/db.cjs 的迁移列表

# 6.4 对已有库执行 003 迁移
#     北极星实现/backend/db/003_add_x_platform.sql

# 6.5 重启 8095，然后验证
#     预期：platform=x → HTTP 200（当前是 400）
```

> ⚠️ 执行 ①②③ 前**建议先备份**。③ 之后**必须重跑门禁**确认变绿。

## 4.10 待产品负责人拍板（**2026-09-15 已逐条定案**）

| # | 事项 | 结论 |
|---|---|---|
| 1 | X 补丁落地 | ✅ **已定**：做「X 源码化」（源码改 4 处 + 003 进迁移清单） |
| 2 | 删废弃直连配置 | ✅ 已定（安全项，命令见 §4.9-①） |
| 3 | 收口源目录 `.fixed.js` | ✅ 已定 |
| 4 | 接真 Ryze | ✅ **已定：暂缓**（继续用仿真数据） |
| 5 | 4.1 剩余 3 项工程 | 挂起（随多租户一并推进） |
| 6 | 历史快照清理 | ✅ 已定（命令见 §4.9-④） |
| 7 | **多租户 BYOK** | ✅ **已定**：BYOK + AES-256-GCM 加密落盘 + 产品级（1 用户 = 1 租户）；主密钥 DPAPI。见 §4.13 |
| 8 | **归因数据多租户口径** | ✅ **已定：B 方案**（先共享演示数据，接真数据后再按租户隔离） |
| 9 | **归因面板 4 模块** | ✅ **已定**：调用渠道/模型明细没数据就空处理 |

## 4.11 验收机制（沿用设计阶段）

1. **实现者 ≠ 验收者**：每个实现单元由**独立验收子agent**验收，**通过才解锁下游**
2. **验收标准前置量化**（对应设计单元的 4 条标准 + 代码可运行/可测试）
3. **返工闭环**：不通过 → 返工 → **重新独立验收**（不是自己说改好了就算）
4. **验收要"试图证伪"**：不接受论断，也不无理由怀疑，而是**自己构造反例去试**
5. 全部记录进本文档第一层（时间线）

**为什么这套机制值**：11 个真问题里**没有一条是"代码跑不起来"**，全部是"**能跑，但会出事**"。
只做"能不能跑"的验收，这 11 个**一个都抓不到**。

## 4.12 当前六技能状态速查

| # | 技能 | 代号 | 状态 |
|---|---|---|---|
| 1 | 打点推荐 | — | ⚪ 未做 |
| 2 | 埋点分析 | `tracking-quality` | 🟢 **完成 + 独立验收** |
| 3 | 对话 | — | ⚪ 未做 |
| 4 | 看板解读 | `dashboard-interpret` | 🟢 完成 |
| 5 | 解析 | — | ⚪ 未做（依赖真 Ryze） |
| 6 | 策略建议 | `ad-strategy` | 🟢 **完成 + 独立验收** |

**另外 10 个社媒侧技能**（`scrape-x` / `scrape-reddit` / `scrape-tiktok` / `scrape-instagram` / `scrape-telegram` /
`analyze-influencer` / `analyze-hashtag` / `monitor-hashtag` / `detect-accounts` / `advice-engine`）
—— **属于另一个项目（社媒增长）合并进来的资产业务线，本次未纳入验收。**

---

## 4.13 ★ 多租户方案与新增需求（v1.2 新增）

- **决策文档**：`北极星产品/架构-多租户与BYOK.md`
- **问题与缺口总清单**：`北极星-问题与缺口总清单-v1.0.md`
- **核心**：BYOK（用户自带 PT key）+ AES-256-GCM 加密落盘 + 产品级（账号/key/会话全进 PostgreSQL）；主密钥 DPAPI；1 用户 = 1 租户。
- **关键机制（已核实 DSH 源码）**：① 凭据「环境变量」始终优先于文件（按次注入用户 key）② 会话/技能按「工作区路径」隔离（每租户一个 workspace 目录即可）③ 数据层 RLS 已实现（`FORCE ROW LEVEL SECURITY` + `set_config('app.current_tenant_id')`）。
- **归因数据口径**：B 方案 —— 先共享演示数据，接真数据后再按租户隔离。
- **归因面板新增 4 模块**：调用渠道 / 全局模型明细 / 国家漏斗对比 / 国家下钻；调用渠道与模型明细**没数据就空处理**。
- **拆成 U1~U5 五个验收单元**，实现者 ≠ 验收者。

---

## 附：本文档版本历史

| 版本 | 时间 | 变化 |
|---|---|---|
| v1.0 | 2026-09-14 | 首版建立，收编 v1.44 及全部 `DSH基座-*` 进度文档 |
| **v1.1** | 2026-09-14 | ① 修自我引用错误 ② 补登记 `北极星总进度-v2.0.md` 为已过期 ③ 单元 X 验收结果（通过 + 3 瑕疵）④ 新增铁律 #14 ⑤ 新增 P0：X 补丁落地 ⑥ 新增转折点 6（为什么验收方要造反例） |
| **v1.2** | 2026-09-15 | ① 收编《问题与缺口总清单 v1.0》与《多租户与 BYOK 决策》② 进度线新增 4 个开发任务（门禁 v4 / X 源码化 / 归因 4 模块 / 多租户 U1-U5）③ 记录多租户决策（BYOK + DPAPI + 产品级；归因数据 B 方案；调用渠道/模型明细空处理；1 用户 = 1 租户；真 Ryze 暂缓）④ 更新门禁状态（v3 9过2提醒0失败，但独立验收指出 4 个子问题）⑤ 新增已知缺陷（key 泄漏、门禁假阴性、Markdown、262s 延迟、社媒 10 技能未接入） |
| **v1.3** | 2026-09-15 | 批 1 收口：① 门禁四轮迭代至 v7 通过（独立验收，剩余为文档化边界）② X 源码化 6 处补丁落源码并通过三重独立验证 ③ 归因 4 模块进入实现 |
| **v1.4** | 2026-09-15 | 三批全部收口：① 批 1 门禁 v7 + X 源码化 ② 批 2 归因 4 模块（真实数据 + 空处理）③ 批 3 多租户 U1~U5（身份库/key加密/租户隔离/前端/收口，五单元独立验收全过） |
| **v1.5** | 2026-09-15 | 新增 §3.3b 生产发布缺口清单（自查 18 项：注册页/设置入口/needKey提示/会话持久化/看板隔离/HTTPS/部署/备份/监控等，按 P0/P1/P2 分级）；PM 指出当前偏 demo 级，需补用户页面与运维生产化 |
| **v1.6** | 2026-09-15 | P0 基础用户页面补齐（注册/Key入口/needKey 提示）实现 + 独立验收通过（29/29 自测 + 端到端）；§3.3b P0 四项标记已补齐（待落地）；修落地说明启动命令 P1（server-v2.cjs） |
| **v1.7** | 2026-09-15 | P1 服务端硬化（会话持久化/登录防爆破/API限流）实现 + 独立验收通过（43/43 自测 + 独立 17 项）；§3.3b #5/#8/#9 标记已补齐（待落地） |
| **v1.8** | 2026-09-16 | P1 收尾批次：① §3.3b 全部 P1 标「✅ 已补齐」（#6 标已实现默认关数据侧待接入、#10 标模板已出域名/证书待填）、新增 P1-1「反代后 IP 塌缩」标已修 ② 时间线新增 4 条（#50-#53，每条带实现者 agent / 验收者 agent / 结论，含「不通过→修复→复核通过」两处）③ 汇总 P2 验收非阻塞项 + 新增「诚实边界 / 待用户提供」醒目区块 ④ 成果线补录 P1 收尾交付物 |
| **v1.9** | 2026-09-17 | 对话能力改造：多对话 / 复制 / 时间 / 重新生成 / 停止 / 并发异步化 / 持久化；conversations 表改「1 对话 = 1 行」；新增对话 CRUD + `/api/chat/stop`。面板分享、流式输出、换库 PostgreSQL 列为本期待做 |
| **v1.10** | 2026-09-17 | ① DSH 调用层迁移：headless 一次性 spawn → 常驻 agent（SDK 模式，`persistent-runner.mjs`），四要素（session 持久 / 轨迹可见 / 逐字流式 / 每用户独立 session）全部验证，冷启动 6338ms→306ms ② ⑫ 面板分享与授权完成（只读 + 整面板，7/7）③ 待办：PT key 余额见底（真实多轮上下文待补跑）、迁移 v1 限制（单进程顺序队列、切模型对新对话生效） |
| **v1.10 续** | 2026-09-20 | ① PT key 充值（新 key 尾号 `IQi2`）后真实 LLM 验证全过（多轮续接/逐字流式/进程复用/轨迹可见/停止/崩溃恢复）+ 修复 3 缺陷（`6706bd8`/`bf53b69`）② ⑬ PostgreSQL 端到端实测通过（zonky 便携 PG 18.6，6 迁移 8 表落库重启不丢）③ ⑤ 分岔/重新生成 bug 修复（`closeDshSession` 重置会话，`83756af`），⑤分岔/⑨图片/⑪记忆真实 LLM 重测通过；v1 限制保留（非 bug） |
| **v1.11** | 2026-09-21 | 安全审查 15 项全部完成并提交：P0 六项 `31a5c58`、P1 五项 + NEW-1 + P2 三项 `e0ebe47`；8 项已验证、6 项代码完成但未 e2e 实测、1 项文档完成；决策：#9 X Ads 补全 X 支持、#10 看板 README 标注骨架版 |
| **v1.12** | 2026-09-21 | Slice 0/1/1.5/2 接缝改造（绞杀者模式）：Slice 0 回归基线（20/20）→ Slice 1 Storage Contract → Slice 1.5 锁基线 + 真实 DB smoke（14/14）+ 硬编码 PT key 清理 → Slice 2 DSH Adapter（Runtime Contract + 真实/mock 适配器 + `@deepseek-ai/dsh@0.1.1-rc.2` 锁定）；seam 19/19 + runtime 26/26 + tenant-mock 14/14 全绿；push 至 origin/main = `fbe874a`。注：因远端 `4504262 update PRD` 做了 rebase，本地旧 hash（31a5c58/e0ebe47/4589d61/d8c5ece/f4a5206/cd83ba0/3b7587a/a060059）对应新 hash（20ae7bc/4d0fd60/7890493/21e5ac5/3243dae/bd03a03/a2cee56/9325d4c） |


---

# ★ v1.11 增量：安全审查结论 + 修复计划（2026-09-21）

> 本增量按四层架构组织，应并入对应层。15 项安全修复（P0 6 / P1 5 / P2 3 / 新增功能 1）已**全部完成并提交**（commit `31a5c58` / `e0ebe47`）；其中 8 项「已验证」、6 项「代码完成但未 e2e 实测」、1 项「文档完成」，逐项标注见第三层进度线。

## 🏷 版本与发布记录（更新）

| 版本号 | 功能 | 发布日期 |
| --- | --- | --- |
| （无版本号） | 15 项安全修复完成：P0 六项 commit `31a5c58`；P1 五项 + NEW-1 + P2 三项 commit `e0ebe47`（main 分支，推到 origin 之后新增 2 个 commit） | 2026-09-21 |

## 第一层 · 时间线（新增 2 条 · 最新在前）

**① 15 项安全修复全部完成并提交（09-21）**：
- **提交记录**：P0 六项 commit `31a5c58`；P1 五项 + NEW-1 + P2 三项 commit `e0ebe47`（main 分支，推到 origin 的历史之后新增 2 个 commit）。
- **状态口径（诚实区分）**：8 项「已验证」、6 项「代码完成但未 e2e 实测」、1 项「文档完成」，逐项标注见本增量第三层进度线。
- **过程发现（如实记录）**：P0-2 初版遗漏了 `start.cjs` 里 `seed()` 调用未传 `webhookSecret` 的 bug——启动 analytics 实测时暴露，已修复并纳入 `e0ebe47`。

**② 安全审查结论 + 修复计划（09-21）**：
- **起因**：对 GitHub 仓库做安全/代码审查，按「安全风险审查意见」整理出 15 项（P0 6 / P1 5 / P2 3 / 新增功能 1）。
- **决策（已拍板，写进进度线）**：
  - **#9 X Ads**：**补全 X 支持**（不是删除选项）—— schema/种子/查询/平台映射全部加 `'x'`（呼应 §4.9 ⑥ 已有 X 补丁落地步骤）。
  - **#10 看板**：**README 标注骨架版 + 列清未实现 Tab**，不做完整看板（当前重点在基座）。
- **影响文件**：见成果线（15 项修复涉及的文件，commit `31a5c58` / `e0ebe47`）。

## 第二层 · 成果线（新增）

- **代码成果（15 项修复，commit `31a5c58` / `e0ebe47`）**：`persistent-runner.mjs`（abort 命令 → `agent.cancel({kind:'user'})`）、`tenant.cjs`（job.kill/超时改「先 abort、3 秒未收敛才杀进程」）、`start.cjs`（`seed()` 补传 `webhookSecret`）、上传落 `/uploads/<tenantId>/<128位随机名>`、`scripts/ci-check.mjs` + `.github/workflows/ci.yml`、README「看板现状（骨架版）」表。
- **验证件**：P0-1/P0-2/P0-6、P1-1/P1-2/P1-3、P2-1/P2-3 已验证；P0-3/P0-4/P0-5、P1-4/P1-5、NEW-1 为「代码完成但未 e2e 实测」（逐项证据见第三层进度线）。

## 第三层 · 进度线（更新）

- **已完成 ✅**：15 项全部完成并提交（P0 六项 commit `31a5c58`；P1 五项 + NEW-1 + P2 三项 commit `e0ebe47`）。状态口径：8 项「已验证」、6 项「代码完成但未 e2e 实测」、1 项「文档完成」。
- **清单状态（15 项 · 全部已完成）**：

  **P0（6 项）**：
  1. ✅ 已验证 — 默认管理员密码固定值 `northstar` → 未设 `PT_SHELL_PASSWORD` 留空启动打印随机密码、登录可用；非回环 + 无密码拒绝启动。
  2. ✅ 已验证 — secret 环境变量化：analytics 缺 `PT_DASH_TOKEN`/`PT_DASH_WEBHOOK_SECRET` 拒绝启动 exit 1；shell 的 `DASH_TOKEN` 去掉默认值，反代未配置时 503、配置后 200；日志只打 hash 前缀。
  3. ⚠️ 代码完成（反代链路验证通过，未浏览器 e2e）— 看板 token 改服务端注入 `Authorization` header + `Referrer-Policy: no-referrer`（URL 去掉 `pt_ro_token`）。
  4. ⚠️ 代码完成（未 e2e 实测）— Cookie 加 `Secure` + `__Host-` 前缀 + HSTS（`PT_COOKIE_SECURE=1` 时生效）。
  5. ⚠️ 代码完成（未 e2e 实测）— shell/analytics 端口一致性（`isFree` 已确认存在；analytics 端口被占即失败，或写运行时端口文件）。
  6. ✅ 已验证 — 前端平台/国家筛选真传参：`platform=meta,google` 多值后端 200；单值/多值/非法值解析均正确。

  **P1（5 项）**：
  7. ✅ 已验证 — CSRF/Origin 校验：伪造跨站 `Origin` 的 POST → 403。
  8. ✅ 已验证 — 上传租户归属/高熵随机名/魔数校验/配额/鉴权读取：非图片 400「不是有效的图片文件」；PNG 200 返回 `/uploads/<tenantId>/<128位随机名>`；无 session 读取 403。
  9. ✅ 已验证 — innerHTML XSS → `textContent` 建节点 + CSP：响应头含 CSP / `X-Content-Type-Options: nosniff` / `X-Frame-Options: DENY`；技能/状态列表改 `textContent` 构建 DOM。
  10. ⚠️ 代码完成（未 e2e 实测真实对话停止）— DSH 超时取消协议：`persistent-runner.mjs` 新增 abort 命令 → `agent.cancel({kind:'user'})`，abort/ping/exit 立即处理不被运行中 task 卡住；`tenant.cjs` 的 job.kill()/超时改「先 abort、3 秒未收敛才杀进程」。
  11. ⚠️ 代码完成（未 e2e 实测）— 异常信息脱敏：统一返回「服务器内部错误」，明细只写服务端日志。

  **P2（3 项）**：
  12. ✅ 已验证 — CI/lint/安全测试：新增 `scripts/ci-check.mjs` + `.github/workflows/ci.yml`，本地跑 115 个 JS 文件 0 失败。
  13. ✅ 完成（文档）— 看板 README 骨架版标注：README 新增「看板现状（骨架版）」表，列清 5 Tab 状态 + 4 条未实现/占位。
  14. ✅ 已验证 — X Ads 补全：seed 补 X 账户 `act_9100011` + 系列 `c_2004` + 素材 `cr_1005` 落到 `platform=x`；`funnel?platform=x` 返回 2 个 X 分组（`orphan_creative_zz` 未命中 + `img_brand_awareness` 有花费无充值）。

  **新增功能**：
  - **NEW-1** ⚠️ 代码完成（未浏览器实测）— 对话框 Ctrl+V 粘贴图片：textarea paste 事件读剪贴板 `image/*` → 复用上传逻辑。

## 第四层 · 全量信息模块（交叉引用）

- 交叉引用：本增量 P0/P1 涉及 §3.3b「生产发布缺口清单」#5/#7/#10/#11/#13/#14 的进一步硬化（会话持久 / key 校验 / HTTPS / 日志 / master key / 部署）；安全项（密码 / secret / cookie / 端口 / CSRF / XSS / 上传 / 异常）与 §4.8「部署与隔离」、§4.9「环境与命令」呼应。
- **决策落点**：#9 X Ads 补全 X 支持 → §4.9 ⑥ 已有 X 补丁落地步骤；#10 看板 → README 标注骨架版（对应 §3.3 待做「看板完成度」）。

---

# ★ v1.10 增量：DSH 调用层常驻 agent 迁移 + 面板分享落地（2026-09-17 ~ 09-20）

> 本增量按四层架构组织，应并入对应层。实现 + 独立验收 + 端到端实测全部通过（面板分享 7/7、冷启动 6338ms→306ms）。**09-20 续**：PT key 充值后真实 LLM 验证闭环 + ⑬ PostgreSQL 实测通过 + ⑤ 分岔 bug 修复。

## 🏷 版本与发布记录（更新）

| 版本号 | 功能 | 发布日期 |
| --- | --- | --- |
| v0.3.0 | DSH 调用层迁常驻 agent（SDK 模式，四要素验证）+ ⑫ 面板分享与授权（只读 + 整面板） | 开发完成 2026-09-17，待发布 |

## 第一层 · 时间线（新增 4 条 · 最新在前）

**① ⑬ PostgreSQL 实测通过 + ⑤ 分岔 bug 修复（09-20）**：
- **⑬ PostgreSQL**：本机 zonky 便携 PostgreSQL 18.6（临时起、不装系统服务）端到端实测——6 个迁移（001~006）8 张表（users/tenants/sessions/api_keys/conversations/user_memory/panel_shares/schema_migrations）全跑通（`db.exec` 多语句 + `gen_random_uuid()` + JSONB 验证）；注册/登录/建对话真实落库（回查 users、conversations 行存在）；重启服务登录态与对话仍在。连接仍是 `PT_PG_*` 环境变量（`PT_PG_DSN` 或 HOST/PORT/USER/PASSWORD/DATABASE + 可选 `PT_PG_LIB` 覆盖 pg 驱动入口）。
- **⑤ 分岔/重新生成 bug**：分岔/重新生成会截断 DB 历史，但热会话里 DSH session 仍持有完整轨迹 → 分岔后仍「记得」分岔点之后内容（真实 LLM 复现：仍提本应丢弃的「99」）。修复：分岔/重新生成时先 `closeDshSession(conversationId)` 重置会话，下一问冷启动、只重放截断后历史。验证：修复后不再提「99」，追问回「42」。commit `83756af`。
- **附带重测**：⑤分岔 / ⑨图片 / ⑪记忆 三条在常驻架构下真实 LLM 重测通过（⑨：LLM 读到上传 1×1 PNG 文件元信息；⑪：跨对话召回「星辰科技」）。

**② 真实 LLM 验证闭环 + 3 缺陷修复（09-20）**：
- **PT key 已充值**（新 key 尾号 `IQi2`），真实 LLM 验证全部通过：多轮续接（第 2 问召回「星辰科技/跨境电商」）、逐字流式（真实 delta 帧 55~92 帧/轮）、进程复用（冷启动消除：首问 7~12s → 追问 ~1.8s）、轨迹可见（glob/web_search/pwsh/read 透传为 step 帧）、停止（stopped=true、无残文）、崩溃恢复（杀进程后下一问冷启动 + 从 DB 重放历史 + 召回上文）。
- **本轮修复 3 个真实 LLM 才暴露的缺陷**：
  - `fix: 常驻 runner 未注入 question`（导致「用户现在问：」后为空）—— commit `6706bd8`
  - `fix: 崩溃恢复`（冷启动复用 conversationId 作 sessionId 与磁盘持久化日志冲突 → 改为冷启动用全新 sessionId）—— commit `bf53b69`
  - `fix: persona`（`shell/agent-persona.md` 开头混入 `https://github.com/tirth8205/code-review-graph` URL，已移除）—— commit `bf53b69`

**③ ⑫ 面板分享与授权：单独排期 → 已完成**（2026-09-17）：
- **语义**：只读 + 整面板（用户此前明确答复）。
- **能力**：创建分享（可选有效期）/ 列表 / 校验 / 撤销；分享链接匿名可访问（无需登录），落地页顶部「只读分享视图」横幅 + 全屏只读看板 iframe；`/api/analytics` 支持登录用户或有效分享 cookie 两种只读回退。
- **实测**：创建→列表→校验→匿名落地页→撤销→撤销后失效，**7/7 通过**。
- **新文件**：`shell/panel-share.cjs`、`shell/db-migrations/006_panel_shares.sql`。
- **commit**：`2d54138`。

**④ DSH 调用层迁移：headless 一次性 spawn → 常驻 agent（SDK 模式）**（2026-09-17）：
- **做法**：直接用 DSH SDK 层程序化 API（`agents.create` / `followup` / `whenIdle` / session/event 流），写正式 Cordis 插件 `persistent-runner.mjs`，不再是 patch node_modules 的 headless 一次性 spawn。
- **架构**：每租户一个常驻 DSH 进程（1 用户 = 1 租户，进程隔离 key 与 cwd）；DSH session 拥有对话轨迹，业务壳在「热会话」时不重复传历史，只在冷启动（首次/进程重启后）重放历史。
- **四要素全部验证**：session 持久（同一 conversationId 复用同一 session，实测两问同一 sessionId）/ 轨迹可见（tool/call + tool/result 透传前端 + steps 落库）/ 逐字流式（assistant/chunk text-delta 继续 SSE）/ 每用户独立 session（每租户进程 + 每对话 session）。
- **实测证据**：连续两问，第 1 问 6338ms（含冷启动）→ 第 2 问 306ms（复用常驻进程），冷启动消除约 6 秒；流式、多轮累加正常。
- **commit**：`80e644a`（迁移）、`a6526e7`（轨迹可见）。

## 第二层 · 成果线（新增）

- **代码成果**：`persistent-runner.mjs`（常驻 agent Cordis 插件）、`shell/panel-share.cjs`、`shell/db-migrations/006_panel_shares.sql`。
- **代码成果（09-20 修复）**：`persistent-runner.mjs`（注入 question / 崩溃恢复 sessionId）、`shell/agent-persona.md`（清 URL）、分岔/重新生成重置会话逻辑。commit `6706bd8` / `bf53b69` / `83756af`。
- **验证件**：面板分享端到端 7/7；常驻进程冷启动 6338ms→306ms 实测；四要素逐项验证。
- **验证件（09-20 真实 LLM / PG）**：PT key 充值后多轮/流式/进程复用/轨迹/停止/崩溃恢复全过；⑬ zonky PG 18.6 迁移落库重启全过；⑤分岔/⑨图片/⑪记忆真实 LLM 重测通过。

## 第三层 · 进度线（更新）

- **已完成 ✅**：
  1. **DSH 调用层常驻 agent 迁移**（SDK 模式，四要素全部验证：session 持久 / 轨迹可见 / 逐字流式 / 每用户独立 session）。
  2. **⑫ 面板分享与授权**（只读 + 整面板，7/7）。
  3. **真实 LLM 验证闭环（09-20）**：PT key 充值（新 key 尾号 `IQi2`）后多轮续接 / 逐字流式 / 进程复用 / 轨迹可见 / 停止 / 崩溃恢复全部通过；修复 3 缺陷（commit `6706bd8`、`bf53b69`）。
  4. **⑬ PostgreSQL 切换实测通过（09-20）**：zonky 便携 PG 18.6 上 6 迁移 8 表跑通、注册/登录/建对话真实落库、重启不丢。
  5. **⑤ 分岔/重新生成 bug 修复（09-20）**：`closeDshSession` 重置会话，修复分岔后仍记得分岔点后内容（commit `83756af`）；⑤分岔 / ⑨图片 / ⑪记忆真实 LLM 重测通过。
- **待做 / 风险 ⚪（如实记录）**：
  1. ~~**PT key 余额见底 → 真实多轮上下文验证待补**~~ → **✅ 已闭环（09-20）**：真实 LLM 验证全部通过（多轮续接召回「星辰科技/跨境电商」、逐字流式 55~92 帧/轮、进程复用首问 7~12s→追问 ~1.8s、轨迹可见 glob/web_search/pwsh/read 透传 step 帧、停止 stopped=true 无残文、崩溃恢复冷启动 + DB 重放 + 召回上文）。
  2. **迁移的 v1 限制（非 bug、属设计限制，保留）**：一个租户进程同时只处理一轮（顺序命令队列）；切模型对「新对话」生效，老对话当前 session 仍是老模型。

## 第四层 · 全量信息模块（更新）

- **部署形态 / 架构**：DSH 调用层由 `dsh --profile headless` 一次性 spawn 迁移为「**常驻 agent（SDK 模式）**」——每租户一个常驻 DSH 进程，SDK 程序化 API 驱动，session 持久 + 轨迹可见 + 逐字流式 + 每用户独立 session（详见 §4.8）。
- **API 契约**：面板分享新增 创建（可选有效期）/ 列表 / 校验 / 撤销；`/api/analytics` 只读回退支持「登录用户」或「有效分享 cookie」两种。

---

# ★ v1.9 增量：对话能力改造（2026-09-17）

> 本增量按四层架构组织，应并入对应层。实现 + 独立验收 + 浏览器端到端测试全部通过。

## 🏷 版本与发布记录（新增板块）

| 版本号 | 功能 | 发布日期 |
| --- | --- | --- |
| v0.1.0 | 初始版：业务壳（注册/登录/绑 key/对话）+ 看板后端（多租户隔离），跨平台 Windows/macOS | 待发布 |
| v0.1.1 | 修复：对话历史登录后不显示（新增 GET /api/chat/history + 前端加载历史） | 待发布 |
| v0.2.0 | 对话改造：多对话 + 复制/时间/重新生成/停止 + 并发异步化 | 开发完成 2026-09-17，待发布 |

> 仓库尚未 push 到 GitHub，故「发布日期」均为待发布；每次真正发布后回填具体日期，并在表末新增一行。

## 第一层 · 时间线（新增 1 条）

**对话能力改造**（2026-09-17）：
- **起因**：用户反馈「退出重进对话就没了」→ 澄清后明确真正需求是「多对话 + 每条消息复制/时间/重新生成 + 停止 + 并发」。
- **决策**：归档 / 流式输出放二期；权限拆两件事（对话私有不动、面板分享单独做大特性）；数据库这轮不动 PGlite（换独立 PostgreSQL 单独排期）。
- **影响文件**：`server.cjs` / `conversations.cjs` / `tenant.cjs` / `public/app.js` / `public/index.html` / `public/styles.css` + 新增 `db-migrations/004_conversations_v2.sql`。

## 第二层 · 成果线（新增）

- **代码成果**（`pt-test\conv\`）：`server.cjs`、`conversations.cjs`、`tenant.cjs`、`app.js`、`index.html`、`styles.css`、`004_conversations_v2.sql`、`自测-对话.cjs`、`落地说明.md`。
- **验证件**：实现自测 `自测-对话.cjs`（51/51）、独立验收 `verify-conv\verify-conv.cjs`（46+dbmodel 复核）、浏览器端到端点击测试（复制/重新生成/切换/持久化全通过）。

## 第三层 · 进度线（更新）

- **已完成 ✅**：对话能力改造（多对话 / 复制 / 时间 / 重新生成 / 停止 / 并发异步化 / 持久化）。
- **待做 ⚪ 新增 4 类**：
  1. 对话二期：归档、流式输出、编辑消息、搜索历史、分岔对话、上传文件、切换模型、记忆、分享、导出。
  2. ~~面板分享与授权（独立大特性，跨租户）~~ → **✅ 已完成（只读 + 整面板，7/7）**，见 v1.10 增量。
  3. ~~数据库切换独立 PostgreSQL（独立步骤，观测性诉求）~~ → **✅ 已实测通过（zonky 便携 PG 18.6）**，见 v1.10 增量。
  4. 运行时清理：`runtime/_tmp` 输出累积、profile 固定复用（生产配置）。
- **本次验收非阻断缺陷**：
  1. 空对话 `#hello` 空态在 thinking 等待期未移除（短暂双屏）。
  2. 删除非当前对话会强制把视图切到第一个对话。
  3. 重命名 / 删除用原生 `prompt()` / `confirm()`（可接受）。
  4. 落地说明「保留」清单漏提已移除的旧 `/api/chat/history`、`/api/chat/reset`。

## 第四层 · 全量信息模块（更新）

- **数据模型**：`conversations` 表从「1 用户 = 1 行」改为「1 对话 = 1 行」（`id` UUID / `user_id` / `title` / `messages` JSONB / `created_at` / `updated_at`），去掉 `user_id` 唯一约束，新增索引 `idx_conversations_user_updated`；消息升级为 `{role, text, ts}`。
- **API 契约**：新增 `GET/POST /api/conversations`、`GET/PATCH/DELETE /api/conversations/:id`、`POST /api/chat`（带 `conversationId` + 可选 `regenerate`）、`POST /api/chat/stop`；移除被取代的旧 `GET /api/chat/history`、`POST /api/chat/reset`。
- **停止 / 重新生成语义**：停止 = kill DSH 子进程、用户问题已保存、未完成回答丢弃、可重新生成；重新生成 = 问题不变、只换最后一条助手回复。
- **并发**：`runDshForUser` 由 `spawnSync` 改为异步 `spawn`（不再阻塞事件循环）；同一用户同时只允许一个生成中的对话（409 busy），不同用户并行。

---

# ★ v1.12 增量：Storage Contract + DSH Adapter 接缝（Slice 0/1/1.5/2，2026-09-21）

> 绞杀者模式 M0：把单体业务壳（`tenant.cjs`/`server.cjs`）按 Slice 切成两条接缝——存储接缝（Slice 1/1.5）+ 运行时接缝（Slice 2），实现者 ≠ 验收者、逐 Slice 门禁。全部提交并 push 至 origin/main = `fbe874a`。

## 🏷 版本与发布记录（更新）

| 版本号 | 功能 | 发布日期 |
| --- | --- | --- |
| （无版本号） | Slice 0-2 接缝改造：回归基线 20/20 + seam 19/19 + 真实 DB smoke 14/14 + runtime 26/26 + tenant-mock 14/14 | 2026-09-21 |

## 第一层 · 时间线（新增 1 条 · 最新在前）

**Slice 0/1/1.5/2 接缝改造（09-21）**：
- **起因**：`tenant.cjs`/`server.cjs` 单体、DSH 集成与业务耦合、存储层未分层，无法单测、无法接 mock，需按「产品三层（Agent 基座/业务壳/业务线）+ adapter 胶水」搭接缝。
- **决策（拍板）**：绞杀者模式逐 Slice 推进；先锁基线（`conv/` 权威源码入库）再补真实 DB smoke；Slice 2 只做 DSH Adapter（Runtime Contract + 真实/mock 适配器 + 版本锁定），**不碰** Workspace Sandbox / Tool Registry / 多 Session 并发 / Model Gateway / 附件重构 / 权限系统 / 归因业务接入。
- **影响文件**：`shell/storage/*`（Slice 1/1.5）、`shell/runtime/*`（Slice 2）、`shell/tenant.cjs`、`conv/tenant-v8.cjs`、`conv/run-local-v12.cjs`、`shell/package.json`、`tests/regression/*`。

## 第二层 · 成果线（新增）

- **代码成果**：
  - 运行时接缝：`shell/runtime/contract.cjs`（Runtime Contract）、`shell/runtime/dsh-adapter.cjs`（真实 DSH 适配器）、`shell/runtime/mock-adapter.cjs`（内存 mock）、`shell/tenant.cjs`（重构为业务组装 + runtime 委托）、`conv/tenant-v8.cjs`（版本化源码）。
  - 存储接缝：`shell/storage/contract.cjs`、`shell/storage/index.cjs`、`shell/storage/migrations/001-006`。
- **验证件**：`runtime-contract-test.mjs` 26/26、`tenant-mock-smoke.mjs` 14/14、`seam-test.mjs` 19/19、`seam-smoke.mjs` 14/14（真实 PGlite：migrate 幂等/事务 commit-rollback/连接复用）、回归 20/20。

## 第三层 · 进度线（更新）

- **已完成 ✅**：
  1. Slice 0：B1-B7 根因确认 + 回归基线（20/20 + mock DSH fixture）。
  2. Slice 1：Storage Contract 薄 shim（不改 SQL）。
  3. Slice 1.5：锁基线（`conv/` 权威源入库）+ 真实 DB smoke（14/14）+ 硬编码 PT key 清理（disk 全清，非仅 .gitignore）。
  4. Slice 2：DSH Adapter（代码 + 测试全绿，已 push）。守住行为：多轮对话 / delta-step-done / abort-stop / 分岔 / 重新生成 / 错误处理 / 现有 SSE 兼容。
- **待做 ⚪**：
  1. Slice 2 收尾：live 重启验证（healthz/登录/对话 SSE）+ 真实 DSH 实机验证（**需一把真实 PT key**）+ Slice 2 验收报告。
  2. 后续 Slice（均未开始）：Workspace Sandbox / Tool Registry / 多 Session 并发 / Model Gateway / 附件重构 / 权限系统 / 归因业务接入。
- **本次验收非阻断项**：
  1. 真实 DSH 路径「代码完成但未实机验证」（缺真实 key；mock 路径全绿）。
  2. auth 自持连接 + 仓储未事务化（过渡态，留后续收口）。

## 第四层 · 全量信息模块（更新）

- **Runtime Contract**：接口 `run(spec)/abort(sessionKey)/closeSession(sessionKey)/close()`；事件 `delta/step/done/error/aborted`；spec 部件式（`persona/memory/imageNote/history/question` 等），冷启动注入 history、热启动由 runtime 内 session 持有。
- **DSH 版本锁定**：`@deepseek-ai/dsh@0.1.1-rc.2`（精确锁定，写入 `shell/package.json`）。
- **Mock 切换**：`PT_MOCK_DSH=1` 走内存 mock（无 DSH 无 LLM），`PT_MOCK_DSH_MODE=ok|error|empty|timeout`。
- **Git 状态**：origin/main = `fbe874a`；因远端 `4504262 update PRD` 做了 rebase（9 笔本地提交干净重放、零冲突），旧 hash 与 push 后新 hash 的映射见版本历史 v1.12 行。
