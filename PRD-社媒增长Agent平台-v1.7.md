# 社媒增长 Agent 平台 · 产品需求文档（PRD v1.7）

| 项 | 内容 |
|---|---|
| 文档版本 | **v1.7** |
| 日期 | 2026-09-09 |
| 状态 | **产品形态终稿**（高保真形态基线 + 第一期交付边界锁定） |
| 变更来源 | 2026-09-09 产品讨论锁定：v1.6 基础上补充「能力约定（四样）+ 意图→工具映射 + 新老用户路由 + MCP 授权 + 新技能准入规范」 |
| 前序文档 | `PRD-社媒增长Agent平台-v1.4-grok.md`（v1.4）、`merged-product-prd-v1.3.md`（v1.3）、`growth-loop-merge-architecture-v1.0.md`、`marketing-tracking-platform-final-v2.1.md` |
| 归因约束对齐 | `powertokens-attribution-prototype/docs/PRODUCT-NORTH-STAR.md`、`PRODUCT-PANORAMA.md`（冲突时：平台壳以本文为准，归因尖刀细节以北极星/SCOPE 为准） |

### 变更记录

| 版本 | 要点 |
|---|---|
| **v1.7** | 补 **能力约定（四样：引用/内容生成/结构化/依赖声明）** + 14 能力清单；**意图→工具映射**；**新老用户路由**；**MCP 授权管理**；**新技能准入规范** |
| **v1.6** | 补 **Agent 基座能力**（9 项 + 三档分期）；新增管理域 **🔍 用量与日志** Tab（可观测性第一期轻量版）；明确「用量与日志 vs 执行历史」触发源区分 |
| **v1.5** | ① 纠正 A/B 类客户定义 = **全流程客户 / 检查分析客户**（非"单点 Skill 类型"，非"大客户/普通客户"）；② 单点能力扩为 **Skill + MCP + Workflow 三类**（v1.4 仅 Skill）；③ 新增**演示身份切换开关**（新用户/老用户）；④ 差异化定位锁定 = **专业能力 + 业务线** |
| v1.4 | Shell 通用 Agent 增长基座；可插拔业务线 + 三层 IA；四种用法；调度双概念层；新老用户开始页；北极星仅归因；内容线改"四级下钻"；PT 双重身份；Skill 1–6 分期 |
| v1.3 | 固定模型名 / 3 模型 auto；PowerTokens key；双载体降级；北极星可量化；「调度与执行」合并 |

---

## 0. 如何读本文 / 高保真 vs 第一期交付

### 0.1 读法建议

| 你是谁 | 建议顺序 |
|---|---|
| 产品 / 设计（做高保真） | §0 → §1 → §2 → §3 → §5 → §8 → §11 |
| 研发（排第一期） | §0.2 → §1.6 → §8.1 → §10 → §12 |
| 新人建立全局图 | §0 → §1 → §2 → §11 |

### 0.2 两套范围，必须分开说

| 范围 | 是什么 | 白话 |
|---|---|---|
| **高保真（形态演示）** | 最终 Shell + **≥ 2 条业务线**的完整 UI 形态 | 「产品长什么样」——内容运营线、单点能力、自定义定时占位、新老用户身份都可以画出来 |
| **第一期真实交付** | Shell 最小可用集 + **广告归因业务线**（Token / PT 类型） | 「这周能上线什么」——内容线、单点能力完整产品、自定义定时 = **演示 / P2，不进第一期验收** |

> **结论**
>
> 高保真可以「看起来很满」；第一期验收只认归因闭环。
> **禁止**把高保真里画出来的内容半闭环、单点能力演示当成北极星指标。

### 0.3 与归因原型文档的关系

- 平台级形态、导航、用法、新老用户 → **本文**
- PT Token 闭环的事件契约、P0–P3、最小审计、硬规则 → **北极星 + SCOPE + 全景**
- 面板五 Tab 交互细节 → 归因原型既有文档；本文只引用，不重写字段级规格

---

## 1. 产品概述

### 1.1 一句话定位

**一个通用 Agent 增长基座（Shell）：把「专业能力（skill / mcp / workflow）」编排成「业务线」，供客户整套启用或单点调用——启用后产出数据面板，并在对话工作台里用 LLM 解决日常问题。业务线可插拔，第一期尖刀是广告归因（Token 业态样板）。**

> **差异化定位（相对其他 Agent 产品）**：通用 Agent 给的是「对话 + 插件市场」；我们给的是**专业能力（垂直领域的 skill / mcp / workflow）+ 业务线（把能力编排成可落地的闭环）**。客户既能把一套业务线整套跑起来，也能把其中某个能力单独拎出来用一次。

### 1.2 背景（为什么做成「壳 + 可插拔线」）

早期有两条独立产品叙事：内容运营闭环、广告投放分析闭环。若各自做壳，成员权限、账户授权、对话、调度会重复建设，且无法让客户「先启用一条线、再加第二条」。

产品讨论锁定：**先做通用增长 Agent Shell，业务能力以业务线形式插入。** 高保真用 ≥2 条线证明形态；第一期只把广告归因（Token / PT）挖通。

### 1.3 核心价值

1. **不写代码启用能力**：业务流 = skill + mcp + workflow 的协作包；启用 → 授权 → 调度 → 面板
2. **面板是产物，不是预置死板页**：一业务流实例 → 一数据面板 Tab
3. **专业能力可编排、也可单点**：同一套 skill / mcp / workflow，既能被业务流编排起来整套跑（A 类全流程），也能单独拎出来用一次（B 类检查分析）——**这是相对通用 Agent 的差异化**
4. **数据视图可被可插拔分析 skill 增强**（机器人检测、关联账户等）
5. **零门槛接大模型**：引导注册 PowerTokens → 自动创建 API key → 绑定平台指定模型（固定模型名，或指定 3 个模型 auto 切换）；可自带 token。不消耗平台免费额度
6. **业务资产载体无关**：资产包可适配 DSH / WorkBuddy / 开源等；当期仅实现 DSH 胶水

### 1.4 可插拔业务线（形态 vs 交付）

| 业务线 | 高保真 | 第一期交付 | 产出视图（示意） |
|---|---|---|---|
| 🔵 **广告归因分析** | ✅ 完整形态 | ✅ **真实交付** | 归因面板（素材 → 用户 → 事件下钻） |
| 🟢 **内容运营** | ✅ 完整形态（半闭环 + 创作/发布占位） | ❌ 演示 / P2 | 数据中心（**四级 / 层级下钻**） |
| 以后更多线 | 预留插槽 | 未排期 | — |

> **白话：** 壳是「插座」；业务线是「插头」。高保真要证明插座能插两根以上；第一期只通电一根（广告归因）。

### 1.5 三层信息架构（业务线 → 类型/模板 → 客户实例）

```
业务线（Business Line）
  └─ 业务类型 / 模板（Type / Template）
       └─ 客户实例（Customer Instance）
```

| 层 | 含义 | 例子 |
|---|---|---|
| **业务线** | 产品能力大类 | 广告归因分析、内容运营 |
| **业务类型 / 模板** | 业态与事件/指标映射 | 广告归因下的 `saas_token_marketplace`（Token 业态）；未来可有 ecommerce 等 |
| **客户实例** | 某客户启用后跑起来的一套配置 + 数据 + 面板 | PowerTokens 作为 Token 业态下的**样板客户实例** |

**PowerTokens（PT）在平台中的位置：**

- PT = **广告归因业务线**下、**Token 业态类型**的一个**样板客户实例**
- **不是**整条产品，也不是唯一业务线
- UI 面板命名约定：`广告归因 · Token业态（样板）`（或等价文案），避免写成「PowerTokens 平台」造成身份混淆

> **结论**
>
> 对外讲故事可以说「我们用 PT 跑通了归因」；对内 IA 必须写成「广告归因 → Token 业态 → PT 实例」。
> 北极星文档里的 tip of the spear 仍然成立——那是**归因交付**的尖刀，不是平台壳的全部。

### 1.6 客户分级：A 类 / B 类（使用深度，非权限等级）

> **关键澄清（v1.5 纠正）**：A/B 类客户指**客户怎么用**，不是客户大小、也不是能力权限分级。

| 客户类型 | 定义 | 在做什么 | 对应平台用法 | 对应能力 |
|---|---|---|---|---|
| **A 类客户** | 使用**归因全流程**的客户 | 从埋点接入 → 广告同步 → 归因查询 → 看板分析，跑完整闭环 | **业务流编排**（①②…整套跑） | 完整业务线 |
| **B 类客户** | 只用**归因检查分析**的客户 | 不做全流程接入，只临时做一次检查/分析（如埋点质量、归因诊断） | **单点能力**（单点 Skill / MCP / Workflow） | 原子能力 |

> **白话：** 所有能力对所有客户开放，A 类和 B 类的区别只是「要不要整套跑」。A 类 = 装一整套净水系统；B 类 = 借一次水质检测笔。**不是** A 类能解锁高级功能、B 类不能用。

### 1.7 分期与北极星（可量化，仅归因）

| 阶段 | 内容 | 说明 |
|---|---|---|
| **第一期（真实交付 / 北极星）** | Shell 最小集 + 广告归因业务线（Token / PT 类型） | 当期唯一成功标准；区分胶水 vs 固有 |
| **第二期** | 内容运营业务流真实化；单点能力产品化等 | 北极星验收后扩展 |
| **高保真（演示版）** | 完整 Shell 形态 + ≥2 条业务线 UI | 前端原型；两条线都做实展示；只展示 DSH 载体 |

#### 北极星成功标准（可量化）— **仅广告归因路径**

| # | 指标 | 过关白话 |
|---|---|---|
| 1 | 归因查询端到端延迟 **P95 < 5 秒** | 对话 + 看板查询体验可用 |
| 2 | `event_id` 幂等去重成功率 **≥ 99.9%** | 同一事件重放不双计 |
| 3 | 广告 × 事件关联率 **≥ 90%** | 能被关联上的转化事件占比 |
| 4 | 用户从「启用归因业务流」到「看到首个归因面板」**< 10 分钟** | 含授权 + 配调度；**诚实列出 SS-GTM 双写清单**（见 §8.1），不把「客户侧改 GTM」假装成平台一键完成 |

**明确移出北极星指标：**

- ~~内容运营：抓取→分析→建议半闭环可用~~ → 改为高保真 / P2 演示目标，**不作为第一期过关标准**

> **说明：** 不采用「归因准确率 vs Meta Ads Manager」——口径不同无法客观对比。以幂等成功率 + 关联率 + 延迟 + 启用时长作为可客观度量标准（与归因北极星文档一致）。

---

## 2. 核心概念

### 2.1 业务流三层拆解

> **业务流 = 🔹业务资产包 + 🧩胶水适配层 + ⚙️运行基座**

| 层 | 说明 | 是否载体无关 |
|---|---|---|
| 🔹 业务资产包 | 后端 Workflow REST API + Skill 的 Prompt/Schema + 业务约束 + 数据契约 | ✅ 100% 复用 |
| 🧩 胶水适配层 | 把资产包适配给某个基座（DSH 用 JS 插件 / WorkBuddy 用图形表单） | ❌ 随载体变 |
| ⚙️ 运行基座 | DSH 自建 / WorkBuddy SaaS / 其他第三方 | 当期仅 DSH |

- **skill**：能力单元（抓取、分析、建议、审计、打点推荐…）
- **mcp**：数据源 / 工具连接器（Reddit/X/TikTok、Ryze/Meta 广告、第三方工具）
- **workflow**：确定性编排（采集、同步、归因查询、定时触发的业务流）

#### 双运行载体（当期策略不变）

> **公理：** 业务资产包不感知上层载体；变化点全部收敛在胶水与基座。
> **当期：** 仅实现 DSH 胶水；WorkBuddy / GitHub / 第三方适配只保留接口契约，胶水实现延至 P2。

> **边界锁定（基座只做 DSH）：** 运行基座、业务壳（7 固定 Tab）**只做 DSH 一套，不做多套**。适配其他平台**只发生在业务资产层**（业务流 + 单个 skill/mcp/workflow 的 Prompt/Schema/约束/数据契约），换平台时用目标平台自己的封装方式（WorkBuddy 表单 / GitHub Actions 等）重新适配，**不搬基座、不搬产品壳**。

### 2.2 四种用法（专业能力可编排、可单点）

> 四种用法用 **①②③④** 编号，**不再用 A/B/C/D**（A/B 已专指客户分级，见 §1.6）。

| 用法 | 名称 | 用户在做什么 | 是否产出数据面板 | 典型入口 | 对应客户 |
|---|---|---|---|---|---|
| **①** | **业务流编排** | 启用预置完整业务流（多 skill/mcp/workflow 协作） | ✅ 启用成功后生成面板 Tab | 业务流库 · 编排分区；开始页「启用业务流」 | **A 类（全流程）** |
| **②** | **单点 Skill** | 跑一个独立能力（如埋点质量、打点推荐、博主分析） | ⚠️ **可不建面板**（多为报告/清单产出） | 业务流库 · 单点能力分区 | **B 类（检查分析）** |
| **③** | **单点 MCP / 工具** | 临时调一个连接器或工具（如 Ryze 广告数据、Reddit API） | ❌ 通常不建面板 | 业务流库 · 单点能力分区 + 对话工作台 | **B 类（检查分析）** |
| **④** | **对话 Agent** | LLM + 已授权的 skill / mcp / workflow 组合解题 | 依赖上下文；不强制新建面板 | 对话工作台 | 通用 |

> **白话：**
> ① = 装一整套净水器并接到厨房水龙头（有面板，A 类客户）；
> ② = 单独用一次水质检测笔（可能只有报告，B 类客户）；
> ③ = 借一把扳手拧一下（B 类客户）；
> ④ = 跟管家说话，让他按需调用上面这些。

### 2.3 单点能力 = 同一套资产，拆开单独用

> **核心原则（v1.5 明确）**：业务流里用到的 skill、mcp、workflow **全部拆出来单列**，客户可以单独选用，**不被"必须一起用"绑死**。一起用 = 业务流编排（①）；单独用 = 单点能力（②③）。

```
同一套资产
├─ 编排起来 → 业务流（①，A 类客户整套跑）
└─ 拆开单用 → 单点能力（②③，B 类客户只做一次检查/分析）
    ├─ Skill      （抓取×5、分析×3、建议、审计、打点推荐、埋点质量…）
    ├─ MCP        （Reddit/X/TikTok/IG/TG、Ryze 广告、Collect…）
    └─ Workflow   （采集编排、广告同步、查询服务、Token 审计…）
```

### 2.4 数据视图增强（可插拔分析 skill）

**数据视图 = 基础数据 + 可插拔分析 skill 动态增强**

| 可插拔 skill | 增强效果 | 归属线（示意） |
|---|---|---|
| 检查机器人 | 评论「疑似机器人」+ 帖子「真实互动率」 | 内容运营 |
| 检查关联账户 | 博主「关联的其他平台账户」 | 内容运营 |

---

## 3. 导航与用户状态

### 3.1 左侧导航（固定 Tab + 动态 Tab）

```
【固定 Tab】
🚀 开始            —— 新用户默认落地；老用户作「再启用一条」入口
💬 对话工作台      —— AI 对话助手
🧩 业务流库        —— 编排分区 | 单点能力分区（Skill·MCP·Workflow）
⏰ 调度与执行      —— 业务流调度 + 执行历史（主）；自定义定时占位（次）
🔐 账户与授权      —— 第三方账户 / MCP + PowerTokens（双重身份分节）
👥 成员与权限      —— 谁可以看什么
🔍 用量与日志      —— agent 工具调用日志 + token 计费（可观测性第一期轻量版）

【动态 Tab】
📊 数据面板 × N    —— 业务流启用后自动生成；用户主工作区
```

> Shell 固定 Tab：**对话工作台 + 业务流库 + 调度与执行 + 账户与授权 + 成员与权限 + 用量与日志 + 动态数据面板**。
> 「开始」页是**状态相关的引导面**，服务新用户默认落地与老用户二次启用，不改变固定 Tab 定义。

### 3.2 动态 Tab 规则

- 一个**已启用的业务流客户实例** → 一个数据面板 Tab
- Tab 命名示例：
  - `广告归因 · Token业态（样板）`
  - `内容运营数据中心`（高保真）
- 业务流库点「启用」→ Tab 生成；「停用」→ Tab 收起（数据可按策略保留，UI 收起）

### 3.3 新用户 vs 老用户（默认落地 + 演示切换）

| 用户状态 | 判定 | 默认打开 | 「开始」页角色 |
|---|---|---|---|
| **新用户** | 已启用业务流数 = 0 | **独立「开始」页** | 主落地 |
| **老用户** | 已启用业务流数 ≥ 1 | **最近使用的数据面板** | 次要入口：「再启用一条」 |

#### 开始页卡片（新用户）

| 卡片 | 强度 | 说明 |
|---|---|---|
| **启用业务流** | 主按钮 / 主卡片 | 进入全页或抽屉式向导（**不是**小弹窗） |
| **试用单点能力** | 次 | 进入业务流库单点能力分区（Skill / MCP / Workflow）或直接拉起某个 Demo 能力 |
| **开始对话** | 弱 | 进入对话工作台（可提示：尚未启用业务流时上下文有限） |

- **可选**一次性轻量 coachmark（气泡指引），**禁止**强制模态拦截
- 启用向导 = **全页或大抽屉**，覆盖：选业务线/类型 → 授权 → 调度（若需要）→ 确认启用

#### 演示身份切换开关（高保真专用，v1.5 新增）

> 高保真原型右上角加「**演示：新用户 / 老用户**」切换开关，一键在两个状态间切换，便于演示两种落地形态：

| 开关状态 | 表现 |
|---|---|
| **新用户** | 数据面板空（无动态 Tab）、业务流库全「未搭建」、默认落地「开始」页 |
| **老用户** | 有动态面板 Tab、业务流库有「已搭建」、默认落地最近数据面板 |

> 说明：这是**演示辅助**，不是产品功能；生产环境由「已启用业务流数」自动判定（见上表）。

### 3.4 🔐 账户与授权

- **已授权账户清单**：社媒 / 广告账户 + 第三方 MCP + **分节展示**的 PowerTokens
- **授权范围**：只读/写、字段范围（广告默认只读）
- **状态**：有效 / 即将过期 / 需重验
- **解除授权** + **解除影响提醒**（会影响哪些正在跑的任务）

#### 分节结构（服务 PT 双重身份）

```
账户与授权
├─ 广告与数据源授权（Meta / Ryze MCP / …）     ← 身份① 归因样板客户侧
├─ 内容平台授权（高保真：Reddit / X / …）
└─ 大模型与 PowerTokens Key                    ← 身份② 对话 LLM 计费
     · 平台引导创建的 PT key（默认绑定模型策略）
     · 自带 token（高级）
```

> **白话：** 同一个「PowerTokens」品牌出现两次——一次是「被分析的客户样板」，一次是「给对话买算力的 key」。UI 必须分两节，避免运营同学去「大模型 Key」里找广告授权。

### 3.5 👥 成员与权限

- 角色：管理员 / 运营 / 投放 / 只读
- 权限：可配置业务流 / 可查看某面板 / 可管理账户授权
- 搭建产物（业务流实例、面板）按成员可见范围过滤

---

## 4. 调度与执行专章

### 4.1 为什么保留这个名字

原「定时任务」与「任务中心」边界模糊（配置了定时却不知道下次执行在哪看）。合并为 **「调度与执行」**：左边配频率，右边看每次跑的结果。

### 4.2 两个概念层（主次分明）

| 层 | 名称 | 第一期 | 高保真 | 说明 |
|---|---|---|---|---|
| **主层** | **业务流调度 + 执行历史** | ✅ 交付 | ✅ | 按业务流实例配置刷新；展示每次执行实例与步骤状态 |
| **次层** | **自定义定时任务**（Agent 个人提醒等） | ❌ **非第一期** | ✅ **占位**（Option A） | 类似「个人 cron / 提醒」；与业务流调度视觉分区，避免混为一谈 |

### 4.3 什么是「会被调度」的，什么不是

| 能力 | 是否调度触发 | 说明 |
|---|---|---|
| 广告同步（Ryze → 自有库） | ✅ 是 | 按配置的 cron / 频率拉取 |
| 内容抓取（高保真 / P2） | ✅ 是 | 按频率抓取 |
| Collect Webhook 收事件 | ❌ **不是** | 被 SS-GTM **推送调用**；异步接收 + 幂等；不要做成「定时去拉 Collect」 |
| 归因查询 API | ❌ 不是定时任务 | 同步查询，被面板 / 对话触发 |
| 通用 Agent cron（任意自然语言定时） | ❌ 第一期不做 | 易与业务流调度混淆；次层占位即可 |

### 4.4 主层 UI 与行为

**调度配置：**

- 按业务流实例配置刷新频率：每 30 分钟 / 每小时 / 每日 / 自定义 cron
- 展示下次执行时间、时区（默认用户工作区时区）

**执行历史：**

- **任务 = 一条业务流的一次执行（实例）**
- 先看整条状态，再展开 skill / workflow 步骤
- 状态：运行中 / 成功 / 失败 / 待执行
- 展示：时间、产出摘要、日志入口、最近 N 次

### 4.5 技术方案（不变）

外部调度器（K8s CronJob / Temporal）触发 DSH Headless 执行业务流，结果写入任务历史表。**DSH 内不内嵌定时器**，避免进程重启丢调度状态。
高保真阶段可用前端模拟调度，不做真实 cron。

### 4.6 次层占位（高保真 Option A）

- 在「调度与执行」内用次级 Tab 或折叠区展示「自定义定时（即将推出）」
- 文案说明：个人提醒 / Agent 定时，与业务流调度分离
- **不可**做成可完整假交互的通用 cron 产品（避免用户以为第一期已交付）

---

## 5. 业务流库

### 5.1 总体结构：一个库，两个分区

```
业务流库
├─ 【编排】业务流编排     —— 用法①：预置完整业务流（A 类客户整套跑）
└─ 【单点】单点能力       —— 用法②③：Skill / MCP / Workflow 单独用（B 类客户检查分析）
```

> **锁定：** 单点能力**不**新增顶级导航 Tab；分区落在业务流库**内部**。

### 5.2 编排分区

- 按**业务线**分组：广告归因分析 / 内容运营（高保真）/ …
- 每条展示：名称、描述、所需 skill / mcp / workflow、状态（已搭建 / 未搭建）、所属类型模板标签（如 `Token业态`）
- 操作：启用 → 全页/抽屉向导（账户 + 调度）→ 生成数据面板 Tab；停用 → 收起 Tab
- 内容线「创作」「发布」：标注「规划中」，点进规划说明（不做完整假交互）

### 5.3 单点能力分区（用法②③，B 类客户）

> **v1.5 修正**：单点能力收 **Skill + MCP + Workflow 三类**，不再只有 Skill。

| 类型 | 例子 | 是否建面板 | 高保真 | 第一期 |
|---|---|---|---|---|
| **Skill** | 网站智能打点推荐、埋点质量 AI 分析、博主相关性分析、内容质量分析、每日建议引擎 | 通常否（产出报告/清单） | ✅ 演示运行 | ✅ 打点推荐/埋点质量本期做（Token 业态深做）；其余内容线 P2 演示 |
| **MCP** | Ryze 广告数据、Reddit Data API、X API v2、TikTok API、Instagram Graph API | ❌ 通常不建面板 | ✅ 演示（拉一次数据） | ❌ 演示 |
| **Workflow** | 采集编排、广告同步、查询服务、Token 审计 | ⚠️ 部分可产出数据 | ✅ 演示 | ✅ 查询/审计随归因交付 |

- 单点执行可从库内「运行」或从对话触发（用法④）
- 产出以报告抽屉 / 对话结果 / 一次数据拉取为主；**不要**强行生成空数据面板
- 每个单点能力标注「所属业务线」与「分期」；Skill-1/2（打点推荐/埋点质量）本期深做 saas_token_marketplace，通用一键部署 / 四维审计推迟

### 5.4 与用法④（对话 Agent）的边界

- 单点 MCP / Workflow 既可在库内直接运行，也可在对话里被 @ 或自然语言触发
- 用法④：对话可 @ 业务流或单点能力；权限仍受账户与成员约束

---

## 6. 对话工作台

### 6.1 大模型接入

- MVP：引导注册 **PowerTokens** → 自动创建 API key → 默认绑定平台指定模型（**固定模型名**，如 deepseek-v4；或**指定 3 个模型 auto 切换**）
- 高级：用户自带 token 接其他模型
- 不计平台免费额度（走 PowerTokens 自身充值）

### 6.2 上下文与引用

- **数据上下文选择器（MVP）**：对话顶栏，默认跟随当前数据面板，**仅单面板**；多选留 P1
- **@ 引用**：@ 面板 / 维度 / 某条素材或贴文（MVP 保留）
- 可调用范围：用户已授权的 skill / mcp / 已启用 workflow（用法④）

### 6.3 员工问答与合规

- 数据问答 + 业务问答
- **对话历史保存 30 天，超期自动归档**
- Agent **禁止编造指标**：只能转述 Workflow / Query 返回的事实（见 §9）

### 6.4 与开始页 / 新用户

- 新用户从开始页弱入口进入对话时，应提示「建议先启用一条业务流以获得数据上下文」，但不阻断对话

### 6.5 Agent 基座能力（相对业务壳的另一层）

> **分层：** 业务壳（Shell 固定 Tab）管「长什么样」；**Agent 基座能力**管「agent 能干什么」；业务线管「提供什么专业能力」。三者缺一不可。

#### 6.5.1 为什么需要这一层

对话工作台要成为「合格 agent」（三种形态：直接运行 / @ 调用 / 自然语言调用），底层必须有一套 agent 基座能力支撑，否则对话就只是个聊天窗口。这一层相对「业务壳」独立，是对话、单点能力、业务流被 LLM 调用的技术前提。

#### 6.5.2 九项能力 + 三档分期

| 能力 | 作用 | 第一期 | 说明 |
|---|---|---|---|
| **LLM 接入** | 模型 key、流式输出（SSE） | ✅ 完整做 | PowerTokens 引导注册 → key → 固定模型名 / 3 模型 auto |
| **工具调用（Tool Calling）** | 把 skill / mcp / workflow 封装成 LLM 可调用的 tool | ✅ 完整做 | **agent 命根子**——没有它，单点能力的 @ 调用 / 自然语言调用全落不了地 |
| **上下文管理** | 数据上下文选择器 + @ 引用 | ✅ 完整做 | 单面板上下文（MVP）；多选 P1 |
| **会话管理** | 多会话、持久化、workspace 隔离 | ✅ 完整做 | 鉴权 + 数据隔离 |
| **提示词管理** | 系统提示 + 业务约束注入 | ✅ 完整做（轻量） | 广告只读、不编造指标等约束灌入系统提示 |
| **权限与安全** | 谁能调哪个工具、广告只读 | ✅ 完整做 | 与「成员与权限」联动 |
| **短期记忆** | 当前会话多轮上下文 | ✅ 完整做 | 对话不丢上下文 |
| **可观测性（轻量）** | 工具调用日志 + token 计费 | ⚠️ 轻量做 | 落地为管理域「🔍 用量与日志」Tab；完整 trace/dashboard P2 |
| **可观测性（完整）** | 完整 trace / dashboard / 告警 | ⏳ P2 | 占位 |
| **RAG** | 向量检索，引用非结构化内容 | ⏳ P2 | 第一期用结构化 Query API 替代 |
| **长期记忆** | 跨会话用户画像 / 偏好 | ⏳ P2 | 第一期先做 30 天历史归档（存储，非记忆管理） |

> **三档总结：** **7 项第一期完整做**（LLM / 工具调用 / 上下文 / 会话 / 提示词 / 权限 / 短期记忆）+ **可观测性轻量做**（用量与日志 Tab）+ **RAG / 长期记忆 / 完整可观测性 P2**。
> **原则：** 北极星（归因闭环）是唯一验收标准；agent 基座能力只为「让归因闭环能被对话调用」服务，不摊薄北极星。

#### 6.5.3 可观测性第一期：🔍 用量与日志 Tab

> 管理域新增 `🔍 用量与日志` Tab，是「可观测性第一期轻量版」的落地点。

| 模块 | 内容 |
|---|---|
| **token 用量** | 今日 / 本周 / 本月 token 消耗（按模型）；PowerTokens 计费额度 |
| **工具调用日志** | 时间 / 用户 / 触发的工具（skill·mcp·workflow）/ 输入摘要 / 结果摘要 / 耗时 / token / 状态 |

> **与「调度与执行 · 执行历史」区分：** 执行历史记录**定时触发**的业务流实例；用量与日志记录**对话触发**的工具调用。触发源不同，不要混在一个 Tab。
> **权限：** 默认管理员可见（与成员权限联动）。

#### 6.5.4 工具调用：资产怎么封装成 LLM 能调用的 tool

> 工具调用是「合格 agent」的命根子：没有它，单点能力的 @ 调用 / 自然语言调用（用法②③④）全部落不了地。

**三类资产的封装方式：**

| 资产 | 封装成 DSH Tool 的方式 | 例子 |
|---|---|---|
| **Workflow**（后端 REST） | 胶水定义 tool 的 name / description / parameters（JSON Schema），调用时发 HTTP 请求 | Workflow-查询服务 → tool `attribution_query`（参数：query_type + date_range） |
| **Skill**（Prompt/Schema） | 资产包的 Schema → tool 的 parameters；Prompt 的用途 → tool 的 description；执行 = LLM 用 Prompt 推理 | Skill-埋点质量 → tool `tracking_quality_audit`（参数：站点） |
| **MCP**（连接器） | DSH MCP Client 连接 MCP server，胶水把 MCP tools 注册到 DSH tool 列表 | Ryze MCP → tool `fetch_ad_spend`（参数：账户 + 时间，只读） |

**完整调用链路（6 步）：**

```
用户对话（@ 引用 或 自然语言）
  → ① 意图识别   LLM 判断用户要查什么指标 / 做什么操作
  → ② 工具选择   指标词映射到对应 skill（见 §6.5.4.2 意图→工具映射）
  → ③ 参数补全   缺参弹结构化表单（博主/时间下拉，不自然语言反问）
  → ④ 路由判断   老用户查库 / 新用户实时拉取（见 §6.5.4.3 新老用户路由）
  → ⑤ 调用执行   skill = Schema + Prompt + LLM 推理，内部可调 MCP
  → ⑥ 结构化输出 按 outputType 渲染（report / checklist / data / status）
  → 结果返回 LLM → LLM 转述（不编造，只转述事实）
  → 写入工具调用日志（用量与日志 Tab）
```

**6 个关键设计点：**

| # | 设计点 | 说明 |
|---|---|---|
| 1 | **description 要准** | LLM 靠它判断「何时调哪个 tool」，描述不清会误调用 |
| 2 | **parameters 要严** | JSON Schema 定义参数类型 / 必填，LLM 按 Schema 填参 |
| 3 | **权限过滤** | 每个 tool 标注「谁能调」，权限插件在注册时过滤 |
| 4 | **只读双保险** | 广告 tool 的 description 写「只读」，后端也强制只读 |
| 5 | **降级** | MCP 失败 → Ryze 降级官方 API / CSV，tool 返回降级提示 |
| 6 | **异步 vs 同步** | Collect 是 Webhook（被推送，不是 tool）；查询是同步 REST |

> **⚠️ 实现经验（原型验证教训）：** 对话里的数据查询**不能关键词 if-else 硬编码**，必须和单点能力一样统一走工具调用链路。五条：
> 1. **统一链路**：@指标 / @单点能力 / 自然语言查询 三种触发，都走「🔧 调用工具 → 返回事实 → 转述」，不要出现「调用通用 skill」这种含糊提示。
> 2. **结构化参数表单**：解析出指标但缺对象（如查机器人占比却没指定博主）时，弹**结构化表单**（博主/时间下拉）让用户补参，不要自然语言反问、不要猜默认值；每个能力带 `params` 字段约定（label/type/required/options/default）。
> 3. **意图→工具映射**：指标词不查通用服务，直接映射到对应 skill（机器人占比/互动真实率→机器人分析；内容质量→内容质量分析），只有广告指标（ROAS/CPA/转化漏斗）才走查询服务。
> 4. **新老用户路由**：同一意图，老用户（有业务流数据）查库、新用户（无数据）先补参→MCP 实时拉取→分析；路由判断靠上下文管理（感知用户状态）。
> 5. **事实来源**：答案必须来自工具返回，禁止背答案（呼应 §9 铁律 2「禁止编造」）。

#### 6.5.4.1 能力约定（能力 Schema）：四样约定

每个能力（Skill / MCP / Workflow）必须四样约定齐全，缺一不可被 Agent 可靠调用：

| 约定 | 回答的问题 | 字段 |
|---|---|---|
| ① 引用规范 | 用户怎么 @ 它 | `name` + `aliases`（别名触发词）+ `type` + `lineName` |
| ② 内容生成规范 | 生成什么内容 | `desc`（用途）+ `outputType` |
| ③ 结构化约定 | 输出长什么样 | `output`（结构化数据） |
| ④ 依赖声明 | 需要什么前置能力/数据 | `deps`（数据依赖[含授权] / 上游能力依赖） |

**四种输出类型（outputType）及渲染框架：**

| outputType | 适用能力 | 输出结构 |
|---|---|---|
| `report` | 埋点质量/博主分析/机器人/内容质量/Token审计 | 标题 + 评分条 + KV + 问题清单表格 + 整改建议 |
| `checklist` | 打点推荐/每日建议 | 标题 + 分组清单（停止/新增）+ 备注 |
| `data` | Ryze/Reddit/X/IG/查询服务 | 标题 + KV 数据行 |
| `status` | 广告同步/采集编排 | 标题 + 状态条目列表 |

**14 能力清单（登记到原型 data.js singleAbilities）：**

| 能力 | 类型 | 别名 | 输出类型 | 依赖 |
|---|---|---|---|---|
| 网站智能打点推荐 | Skill | 打点推荐/打点建议/埋点推荐 | checklist | 站点信息 |
| 埋点质量 AI 分析 | Skill | 埋点质量/埋点分析/埋点诊断 | report | SS-GTM 事件数据 |
| 博主相关性分析 | Skill | 博主分析/相关性分析/KOL 分析 | report | 博主贴文数据（scrape） |
| 机器人分析 | Skill | 机器人/水军检测/机器人检测/真实互动率检测 | report | 评论数据（X/IG API，需授权） |
| 内容质量分析 | Skill | 内容质量/内容评分/贴文评分 | report | 贴文数据（scrape） |
| 每日建议引擎 | Skill | 每日建议/建议引擎/运营建议 | checklist | 上游分析结果（相关性/机器人/质量） |
| Ryze 广告数据（只读） | MCP | Ryze/广告数据/广告花费 | data | Ryze 授权（只读） |
| Reddit Data API | MCP | Reddit | data | Reddit OAuth2 授权 |
| X API v2 | MCP | X/推特/Twitter/推文 | data | X 授权 |
| Instagram Graph API | MCP | Instagram/IG/ins | data | IG Graph API 授权 |
| Workflow-查询服务 | Workflow | 查询服务/归因查询/数据查询 | data | Collect + 广告同步（数据已落库） |
| Workflow-广告同步 | Workflow | 广告同步/同步 | status | Ryze MCP（只读） |
| Workflow-Token 审计 | Workflow | Token 审计/审计 | report | Collect 事件 |
| 采集编排 | Workflow | 采集/抓取/采集任务 | status | 5 渠道 MCP 授权 |

#### 6.5.4.2 意图 → 工具映射

指标词不查通用服务，直接映射到对应 skill：

| 用户意图（指标词） | 映射工具 |
|---|---|
| 机器人占比 / 互动真实率 | 机器人分析（Skill） |
| 内容质量 | 内容质量分析（Skill） |
| 博主相关性 | 博主相关性分析（Skill） |
| ROAS / CPA / 转化漏斗 | Workflow-查询服务（归因面板） |

#### 6.5.4.3 新老用户路由 + MCP 授权管理

**同一意图，不同用户状态执行路径不同：**

| 用户状态 | 执行路径 |
|---|---|
| 老用户（已装业务流） | 定时采集已落库 → 查库 → 分析 |
| 新用户（第一次用） | ① 补齐平台/博主/时间 → ② MCP 实时拉取 → ③ 分析结论 |

路由判断 = 上下文管理（感知用户状态）+ 工具调用（选不同工具）。

**MCP 授权管理：** 一次授权 + 定期续期（token 过期时间），登记进「账户与授权」Tab（平台/scope/过期时间/被谁使用）。新用户首次调某 MCP：先授权 → 再拉数据，未授权则卡在授权那一步。

#### 6.5.4.4 新技能准入规范

任何新技能要接入 Agent，三关缺一不可：

1. **四样约定齐全**：引用规范 + 内容生成规范 + 结构化约定 + 依赖声明
2. **登记进能力清单表**（名称/别名/类型/输出类型/依赖）
3. **依赖的 MCP 在账户与授权里登记**（否则无法拉数）

#### 6.5.5 上下文与提示词：约束怎么注入

**上下文管理（单面板 + @ 引用）：**

```
用户选面板（或默认跟随当前面板）
  → 上下文插件存 currentPanel 状态
  → 注入系统提示：「当前数据上下文 = 广告归因面板」
  → 用户问「ROAS 多少」 → LLM 知道该调 attribution_query
  → @ 引用某素材 → 解析成素材 id → 传给 tool
```

**提示词管理（系统提示四段组装）：**

| 段 | 内容 | 来源 |
|---|---|---|
| **[基础]** | 「你是社媒增长 Agent…」 | 平台 |
| **[约束]** | 工程铁律：广告只读 / 不编造指标 / Workflow 是事实源 | **资产包**（胶水引用，不写死） |
| **[上下文]** | 当前数据上下文 = 广告归因面板（Token 业态） | 上下文插件动态注入 |
| **[能力]** | 可调用 tools 列表 + 每个 tool 的 description | 胶水根据已授权资产组装 |

> **关键原则：** 约束文本放**业务资产包**，胶水组装系统提示时引用，不在胶水写死——保证换载体（WorkBuddy/GitHub）时约束不丢。

#### 6.5.6 其余 5 项的实现方案

| 能力 | 实现方案 | 复用/新增 |
|---|---|---|
| **LLM 接入** | DSH 已有模型接入；增量 = PT 授权插件（引导注册 → 回调 → 自动建 key → key 加密存储 → 写死模型名 deepseek-v4 或 3-auto 切换规则） | ✅ 复用 80% |
| **会话管理** | DSH 已有 session；增量 = 会话加 `workspace_id` 维度，一个客户实例 = 一个 workspace，数据隔离 | ✅ 复用 + 扩展 |
| **权限与安全** | DSH 已有鉴权；增量 = 权限插件，tool 注册时标注「谁能调」，运行时按成员角色过滤；广告只读后端双保险 | ✅ 复用 + 过滤 |
| **短期记忆** | DSH 会话已含多轮上下文；增量 = 对话历史落 PG，30 天归档自动清理（存储，非记忆管理） | ✅ 复用 + 归档 |
| **可观测性** | 胶水执行 tool 时记录（时间/用户/工具/输入/结果/耗时/token/状态），token 从 LLM 调用返回取；落 PG，用量与日志 Tab 查询 | ❌ 新增 |

> **3-auto 切换规则（LLM 接入）：** deepseek-v4 → claude-sonnet → qwen-plus，任一超时/限流自动切换下一个；key 加密存储（AES-256）。
> **workspace 隔离（会话/权限）：** 每个客户实例独立 workspace，会话、数据面板、工具调用日志都按 workspace 隔离，越权访问 = 安全红线。

---

## 7. 数据模型

### 7.1 内容运营线：四级 / 层级下钻（改名）

> **命名锁定：** 使用 **「四级下钻」或「层级下钻」**。
> **禁止**再写「四维下钻」——避免与归因全景中的 **「四维审计」**（Config / 运行时 / Server 日志 / 目的地）撞名。

```
平台（顶层）
├─ 博主 → 贴文 → 评论
└─ hashtag → 贴文 → 评论
```

- hashtag 与博主并列，同属平台，都可下钻到贴文
- 贴文以 `platform_id` 为主键；博主 / hashtag 为索引标签，避免冗余

#### 7.1.1 平台维度

| 字段 | 说明 |
|---|---|
| 平台名 | X / Instagram / TikTok / Reddit / Telegram |
| 追踪的 hashtag | 该平台下追踪的 hashtag 列表（含贴文数、互动率） |
| 追踪的博主 | 该平台下追踪的博主列表（简要信息） |

#### 7.1.2 博主维度

| 字段 | 说明 |
|---|---|
| 平台 | 所属平台 |
| 账户分类 | 垂类/类型 |
| 账户年龄 | 注册时长 |
| 粉丝数 | — |
| 互动率 | — |
| 内容数 | 发帖总量 |
| 发帖频率 | 条/周 |
| 互动真实率 | 由「检查机器人」skill 计算：真人互动占比 = 真人评论数 / 总评论数。skill 未启用时显示「—」；启用后按置信度阈值（默认 0.7）标注机器人 |
| 疑似关联账户 | 装「检查关联账户」skill 后显示 |
| 关联账户列表 | 点开展开，展示关联的不同平台账户 |

#### 7.1.3 贴文维度（最小内容颗粒度）

| 字段 | 说明 |
|---|---|
| 发帖时间 | — |
| 互动人数 | — |
| 点赞数 | — |
| 评论数 | — |
| 趋势环比 | 对比前一天 |
| 贴龄 | 发布至今时长 |
| 标签 | topics/hashtag |
| 完整贴文 | 模仿平台样式（头像 + 用户名 + 正文） |
| 点赞互动 | 点赞/评论/浏览/热度 |
| 评论列表 | 每条评论详情 |

#### 7.1.4 评论层

| 字段 | 说明 |
|---|---|
| 用户名 | — |
| 评论内容 | — |
| 点赞数 | — |
| 疑似机器人标签 | 装「检查机器人」skill 后显示（✓真人 / 🤖疑似机器人） |

#### 7.1.5 hashtag 维度

| 字段 | 说明 |
|---|---|
| hashtag 名 | — |
| 贴文数量 | — |
| 互动率 | — |
| 下钻 | → 具体贴文（通过 hashtag 索引关联贴文主键） |

### 7.2 广告归因面板数据模型

- **不在本文重定义字段级规格**；引用既有归因原型与北极星文档
- 五 Tab 顺序：总览 → 素材 → 用户 → 产品 → 投放/接入健康
- Token 业态要点：注册→Key→调用→充值漏斗；同步/异步平均延时（不做 P50）；末触 UTM；面板只查自有库
- Token **最小审计**挂健康页区块（≠ 完整四维审计产品）
- 面板标签：`广告归因 · Token业态（样板）`

### 7.3 数据面板（动态生成）共性

- 启用业务流后自动生成
- 全局筛选与下钻规则由各业务线模板定义
- 可插拔 skill 动态增强视图（内容线示例见 §2.4）

### 7.4 存储分层与每用户配额

**存储分层：**

| 数据 | 存储 | 保留 | 说明 |
|---|---|---|---|
| 元数据（业务流/skill/mcp/workflow 定义） | PostgreSQL | 永久 | 平台级共享 |
| 权限 / 成员 / 会话 | PostgreSQL | 会话结束 | workspace 隔离 |
| 事件（Collect） | ClickHouse（P1）/ PG（MVP） | 90 天 | 大数据量、列式 |
| 广告数据 | PostgreSQL（AES-256 加密） | 账户存续 | 敏感 |
| 社媒贴文 / 评论 | ClickHouse | 90 天 | — |
| 对话历史 | PostgreSQL | 30 天归档 | — |
| 工具调用日志 / token 计费 | PostgreSQL | 90 天 | 用量与日志 Tab |
| 向量索引（RAG） | 向量库 | 与源同步 | P2 |

**每用户配额（第一期机制预留、宽松执行，P2 商业化收紧）：**

| 维度 | 限制 | 第一期 |
|---|---|---|
| 事件量 | 月事件数上限 | 宽松（样板 PT 单租户） |
| 存储 | 每 workspace 存储配额 | 预留字段 |
| 工具调用 | 日调用次数上限 | 防滥用 |
| token | 月预算（PowerTokens 计费） | 计费挂钩 |
| 业务流 | 启用数量上限 | 预留 |
| 保留期 | 事件 90 天 / 对话 30 天 | 自动清理 |

---

## 8. 业务线清单

### 8.1 🔵 广告归因分析线（第一期真实交付）

#### 8.1.1 类型模板与 PT 样板

| 项 | 内容 |
|---|---|
| 业务线 | 广告归因分析 |
| 第一期类型 | `saas_token_marketplace`（Token 业态） |
| 样板客户实例 | PowerTokens（PT） |
| 面板名 | `广告归因 · Token业态（样板）` |
| 接入原则 | 与未来外部客户同路径：SS-GTM 双写 → Collect；**禁止**直连 PT 业务库 |

三维模板 key（终局预留，本期不铺多模板 UI）：
`{business_domain}_{is_independent_site}_{site_builder}` → 本期深做一套 PT 映射。

**business_domain 枚举（本期深做 saas_token_marketplace，其余模板后续铺）：**

| business_domain | 业务说明 | 典型站点 | 核心转化事件 |
|---|---|---|---|
| **saas_token_marketplace**（本期） | Token/API 模型额度转售、按量计费 | powertokens.ai / openrouter.ai / fal.ai / kie.ai | `api_call`、`token_consume`、`credit_purchase`、`balance_recharge`、`usage_exceed` |
| ecommerce | 实物/虚拟商品电商独立站 | Shopify、自建电商 | view_item、add_to_cart、begin_checkout、purchase、refund |
| saas_subscription | SaaS 按月/年订阅付费 | flova.ai、普通工具 SaaS | signup、trial_start、subscribe、subscription_paid、churn_cancel |
| lead_gen | B2B 线索获客官网，无在线支付 | 企业咨询官网 | form_submit、demo_request、book_call |
| content | 内容资讯博客站 | 资讯博客 | page_view、click_download |
| other | 无法识别业务 | — | 基础通用事件集 |

**三维模板 key 展开（终局，本期只深做 saas_token_marketplace 一套）：**

`{business_domain}_{is_independent_site}_{site_builder}`

| 维度 | 取值（枚举） | 本期 |
|---|---|---|
| `business_domain` | ecommerce / saas_subscription / saas_token_marketplace / lead_gen / content / other | 深做 saas_token_marketplace |
| `is_independent_site` | independent（独立站）/ platform（平台店） | 独立站（PT 自建） |
| `site_builder` | 自建 / Shopify / Shoplazza / WooCommerce / … | 自建 |

**硬规则：** GTM 模板与面板指标映射**必须成对发布**——只上 GTM、不上指标映射 = 禁止。

**PT 样板事件映射（本期唯一深做）：**

| 事件 | 说明 | 上报侧 | 归因用途 |
|---|---|---|---|
| `api_call` | 每次模型 / API 调用 | Server（SS-GTM 镜像） | 调用量漏斗 |
| `token_consume` | Token 额度消耗 | **Server（强制）** | 消耗归因 |
| `credit_purchase` | 额度购买 / 充值 | **Server（强制）** | 收入归因（核心） |
| `balance_recharge` | 余额充值 | **Server（强制）** | 充值漏斗 |
| `usage_exceed` | 超额 / 限流 | Server | 健康告警 |

> 强制 Server 侧（经 SS-GTM 进入 Collect）：`credit_purchase`、`balance_recharge`、`token_consume`；`event_id` 必填可检（幂等去重）。

#### 8.1.2 Workflow（5 个，后端 REST / Webhook）

| Workflow | 职责 | 调用方式 | 调度？ |
|---|---|---|---|
| Workflow-Collect | SS-GTM Collect 事件接收、幂等落库 | **异步 Webhook + 幂等** | ❌ 被推送 |
| Workflow-广告同步 | Ryze MCP 只读同步广告落自有库、降级 | REST（同步，< 30s） | ✅ 可调度 |
| Workflow-查询服务 | 归因看板 Query（总览/素材/用户/产品/健康） | REST（P95 < 5s） | ❌ 查询触发 |
| Workflow-Token 最小审计 | Server 事件校验、`event_id` 校验、告警数组 | REST | 可被健康检查触发 |
| Workflow-模板配置 | template-config 输出行业模板 JSON | REST | ❌ |

#### 8.1.3 Skill 1–6：分期标注

| Skill | 名称 | 第一期 | 高保真 | 说明 |
|---|---|---|---|---|
| Skill-1 | 网站智能打点推荐 | ✅ **本期做（saas_token_marketplace 深做）** | ✅ 单点能力演示 | 按 Token 业态模板输出打点清单；**通用一键部署（覆盖全 6 domain）推迟到终局 A 类** |
| Skill-2 | 埋点质量 AI 分析 | ✅ **本期做（saas_token_marketplace 深做）** | ✅ 单点能力演示 | 按 Token 业态模板审计核心事件；**完整四维审计（通用）推迟**，与「最小审计」不同 |
| Skill-3 | 业务对话交互 | ✅ **预留接入 + 对话可用（依赖真数据）** | ✅ | 无稳数据时不做「完整六 Skill 产品」包装 |
| Skill-4 | 看板数据 AI 解读 | ✅ **依赖查询服务真数据** | ✅ | 只转述 Query 结果 |
| Skill-5 | MCP 广告数据解析 | ✅ **只读解读已同步数据** | ✅ | 缺字段降级，不估算 |
| Skill-6 | 广告策略建议 | ✅ **仅建议展示** | ✅ | **禁止写预算 / 启停广告** |

**Skill-5 降级：** Ryze MCP 缺字段（如 `cost_per_click`）时输出「该指标暂不可用」，提示检查授权范围；禁止幻觉补数。

#### 8.1.4 胶水 vs 固有

| 组成 | 固有（资产包） | 胶水（DSH） |
|---|---|---|
| Workflow Collect / 同步 / 查询 / 审计 / 模板 | ✅ REST/Webhook | — |
| Skill 1–6 Prompt / Schema / 约束 | ✅ | — |
| 公共业务约束（禁止写操作等） | ✅ | — |
| 数据契约 JSON | ✅ | — |
| Workflow → DSH Tool 的 JS 插件 | — | ✅ |
| Ryze MCP Client 连接器 | — | ✅ |
| 会话鉴权 / workspace 隔离 | — | ✅ |

### 8.2 🟢 内容运营线（高保真形态 / P2 交付）

| 步骤 | 状态 | 说明 |
|---|---|---|
| 抓取 | ✅ 已有能力 | scrape-reddit / x / tiktok / instagram / telegram |
| 分析 | ✅ 已有能力 | 相关性、机器人、内容质量 |
| 建议 | ✅ 已有能力 | advice-engine |
| 创作 | ⬜ 占位 | content skill |
| 发布 | ⬜ 占位 | 第三方 MCP/API |

- 宗旨：**自己少做功能，优先做接入**
- **第一期不纳入北极星验收**；高保真展示半闭环 + 占位愿景
- 数据面板使用 **四级 / 层级下钻** 表述

### 8.3 PT 双重身份（必须分清）

| 身份 | 出现位置 | 做什么 |
|---|---|---|
| **① 归因样板客户** | 业务流实例 / 数据面板 / 广告与 Collect 授权 | Token 业态样板；SS-GTM 双写、广告只读同步 |
| **② 对话 LLM 计费通道** | 账户与授权 · **大模型 / PowerTokens Key** 专节 | 引导注册 → 自动创建 key → 绑定模型；用户充值额度 |

详见 **§3.4 账户与授权**（分节结构）。

---

## 9. 工程铁律（违反即重做）

| # | 铁律 | 白话 | 反例 |
|---|---|---|---|
| 1 | **业务逻辑不下沉到 Agent** | 埋点评分、告警、聚合在后端 Workflow；Agent 只做自然语言转述 | Agent 自己算 ROAS 写进回复当事实 |
| 2 | **Workflow 是事实来源** | 禁止 AI 编造指标；无数据就空态/诚实说缺 | mock 填满假装生产已通 |
| 3 | **Skill Prompt/Schema 与胶水解耦** | Prompt 在资产包；胶水只引用 | 把业务规则写死在 DSH 插件里 |
| 4 | **Agent 层可插拔** | 换基座不伤核心业务 | 核心校验只存在 Agent 会话里 |
| 5 | **胶水只做组装 / 转发 / 异常** | 不实现业务计算 | 胶水里做归因 JOIN |
| 6 | **广告只读** | 同步花费与素材；不改客户广告 | Skill-6「一键暂停差素材」真写回 |
| 7 | **Skill-6 仅建议** | 策略建议可展示，**无预算/投放写操作** | 自动调预算 |
| 8 | **Ryze MCP 降级** | 失败 → 官方 API 只读和/或 CSV；UI 提示 + 日志 | 面板运行时直查 MCP；无降级 |
| 9 | **当期仅 DSH 胶水** | 双载体可迁移性保留在架构；P2 再写其他胶水 | 同期维护两套胶水摊薄北极星 |
| 10 | **无业务库直连特权** | PT 也走 Collect，与外部客户同路径 | 报表直连 PT 生产库当主路径 |
| 11 | **Token 域禁止纯前端-only 有效转化** | 充值/调用等以 Server 事件为准 | 只用前端 purchase 冒充充值成功 |

---

## 10. 决策汇总表

| # | 决策点 | 结论（v1.5） |
|---|---|---|
| 1 | 产品形态 | **通用 Agent 增长 Shell** + 可插拔业务线 |
| 2 | Shell 组成 | 对话工作台 + 业务流库 + 调度与执行 + 账户与授权 + 成员与权限 + 动态数据面板 |
| 3 | 高保真 vs 第一期 | 高保真 = 壳 + ≥2 线形态演示；第一期 = 壳最小集 + 广告归因（Token/PT） |
| 4 | 三层 IA | 业务线 → 业务类型/模板 → 客户实例；PT = Token 业态下样板实例 |
| 5 | 面板命名 | `广告归因 · Token业态（样板）` 等，避免 PT = 整个产品 |
| 6 | 四种用法 | **①②③④**：① 业务流编排 / ② 单点 Skill / ③ 单点 MCP / ④ 对话 Agent（**不用 A/B/C/D**） |
| 7 | 客户分级 | **A 类 = 全流程客户（走①）**；**B 类 = 检查分析客户（走②③）**；非权限等级，所有能力对所有人开放 |
| 8 | 单点能力范围 | **Skill + MCP + Workflow 三类**，落业务流库内「单点能力」分区，不新开顶导航 |
| 9 | 调度与执行 | 保留名称；主层 = 业务流调度+历史（P1）；次层 = 自定义定时占位（非 P1） |
| 10 | Collect vs 调度 | Collect = Webhook 推送，**不是**定时任务；广告同步/内容抓取才是调度 |
| 11 | 新用户默认 | 独立「开始」页；主：启用业务流；次：试用单点能力；弱：对话；无强制模态 |
| 12 | 老用户默认 | 最近数据面板；开始页变为「再启用一条」 |
| 13 | 演示身份切换 | 高保真右上角「演示：新用户/老用户」开关（v1.5 新增，演示辅助非产品功能） |
| 14 | 启用向导形态 | 全页或大抽屉，非小弹窗 |
| 15 | 北极星指标 | **仅归因**：P95、event_id 幂等、关联率、<10min 启用（含诚实 SS-GTM 清单） |
| 16 | 内容线与指标 | 移出北极星；高保真/P2 演示 |
| 17 | 内容下钻命名 | **四级下钻 / 层级下钻**；不用「四维下钻」 |
| 18 | Skill 1–6 | 1/2 = 本期深做 saas_token_marketplace（通用产品化推迟）；3/4/5/6 按 §8.1.3；完整六 Skill 通用产品化非 P1 |
| 19 | PT 双重身份 | 归因样板客户 **与** 对话 LLM 计费 key **分节** |
| 20 | 对话大模型 | 引导 PT 注册 → 自动 key → 固定模型名或 3 模型 auto；自带 token 高级选项 |
| 21 | 双运行载体 | 架构可迁移；当期仅 DSH 胶水 |
| 22 | 业务流结构 | 资产包 + 胶水 + 基座 |
| 23 | 数据面板 | 业务流产物，一实例一 Tab |
| 24 | 数据视图增强 | 基础数据 + 可插拔 skill |
| 25 | 对话引用 | 单面板上下文 + @（多选 P1） |
| 26 | 任务粒度 | 业务流执行实例，可展开步骤 |
| 27 | 账户授权 | 范围 + 解除影响提醒 + PT 分节 |
| 28 | 成员权限 | 谁可以看什么 |
| 29 | 创作/发布 | 占位愿景，完整假交互不做 |
| 30 | 定时技术方案 | 外部调度器 + DSH Headless；DSH 不内嵌定时器 |
| 31 | 广告边界 | 只读分析；Skill-6 建议 only |
| 32 | Ryze | 只读落库 + 降级；面板不直查 MCP |
| 33 | 隐私合规 | GDPR 保留 + ToS 风险 + 加密审计（附录 A） |
| 34 | 明确不做（P1） | 交汇点打通、A 类一键部署产品化、完整四维审计产品化、Shopify、多租户计费、内容线真交付、第三方载体胶水、多面板上下文、通用 Agent cron |
| 35 | Agent 基座能力 | **9 项**（LLM/工具调用/上下文/会话/提示词/权限/短期记忆/可观测性/RAG + 长期记忆）；**三档**：7 完整做 + 可观测性轻量 + RAG/长期记忆/完整可观测性 P2 |
| 36 | 工具调用 | agent 命根子：skill/mcp/workflow 封装成 LLM tool，支撑单点能力 @ 调用 / 自然语言调用（用法②③④） |
| 37 | 用量与日志 Tab | 管理域新增，可观测性第一期轻量版（工具调用日志 + token 计费）；记录**对话触发**的工具调用，区别于「执行历史」的**定时触发**；默认管理员可见 |
| 38 | 基座边界 | **只做 DSH 一套基座 + 业务壳**；适配其他平台只在**业务资产层**（业务流 + skill/mcp/workflow），不搬基座、不搬产品壳 |
| 39 | 存储配额 | 每用户配额机制预留（事件量/存储/调用/token/业务流数），第一期宽松、P2 商业化收紧 |
| 40 | 第三方限流 | 接入前单独调研（配额+速率+降级）；限流/失败降级或空态，**禁止幻觉补数** |

---

## 11. 用户故事

### 故事 1：新用户 · 开始页首次启用归因（第一期主路径）

> 小红是投放同学，第一次打开平台（0 条业务流）。默认进入 **「开始」页**，看到三张卡片。她点击主卡片「启用业务流」。
>
> 进入**全页向导**（非小弹窗）：
> 1. 选择业务线「广告归因分析」→ 类型「Token 业态」→ 样板说明（或绑定 PT 工作区）
> 2. 授权广告账户（只读）与 Ryze MCP（如需要）
> 3. 配置广告同步频率（默认每日）
> 4. 看到 **SS-GTM 双写清单**（诚实步骤，不假装平台已替她改完 GTM）：
>    - 在现有 SS-GTM 增加 Webhook / 镜像 Tag → 产品 Collect
>    - 确认不影响现有 GA / 广告回传
>    - 用 staging 密钥做一次测试事件，确认 Collect 日志有 `event_id`
> 5. 确认启用
>
> 完成后左侧出现 `广告归因 · Token业态（样板）` 面板 Tab；若事件与广告尚未齐，面板对缺数模块给**空态**而非假数。目标体验：熟练用户在授权与清单配合下 **< 10 分钟**看到面板骨架并理解下一步补数。

### 故事 2：老用户 · 默认进最近面板

> 小红已启用归因业务流。再次登录默认打开最近的归因面板，而不是开始页。她从左侧点「开始」可「再启用一条」（例如高保真下的内容线），开始页此时是次要入口。

### 故事 3：A 类客户 · 全流程（业务流编排）

> A 类客户要跑完整归因闭环，在业务流库「编排」分区启用「广告归因 · Token 业态」，走全页向导（授权 + 调度 + SS-GTM 双写清单），启用后生成归因面板，日常在面板和对话工作台里看数据、问 AI。这是第一期主路径。

### 故事 4：B 类客户 · 检查分析（单点能力）

> B 类客户不做全流程接入，只想临时做一次检查分析。进入业务流库「单点能力」分区，直接运行「埋点质量 AI 分析」，得到问题列表与整改建议（报告抽屉，**不**生成数据面板）。本期按 saas_token_marketplace 模板深做审计；通用四维审计产品化推迟。

### 故事 5：单点能力 · Skill / MCP / Workflow 单独用（v1.5 扩）

> 小白在「单点能力」分区看到三类可单独运行的资产：
> - 运行一个 **Skill**（如「博主相关性分析」）→ 得到一份博主分析报告
> - 运行一个 **MCP**（如「Reddit Data API」）→ 拉一次某 subreddit 热帖数据
> - 运行一个 **Workflow**（如「查询服务」）→ 查一次归因数据
>
> 每个都可单独触发，不被"必须启用整条业务流"绑死；一起用才是业务流（用法①）。

### 故事 6：对话 Agent 引用归因数据

> 小红在对话工作台问：「上周哪个素材注册成本最低？」助手在单面板上下文中调用查询服务，转述事实：「素材 A CPC 注册成本 ¥x，素材 B ¥y。」点击回答中的素材名跳转到面板素材 Tab。Skill-6 若补充策略建议，仅展示文案，**不**提供写回预算按钮。

### 故事 7：管理员权限

> 管理员给运营开通「内容运营面板只读」（高保真），不开放广告账户授权管理与归因面板。成员与权限按面板 / 业务流实例粒度生效。

---

## 12. 技术架构

### 12.1 架构图（相对 v1.3 适配 Shell + 业务线）

```
┌──────────────────────────────────────────────────────────────┐
│              载体（运行环境）：DSH 基座（当期唯一）             │
│   Cordis 内核 · 会话 · 鉴权 · workspace 隔离 · tool 机制 · MCP  │
└──────────────────────────────────────────────────────────────┘
                                ▲
                                │ 胶水（资产包 ↔ 载体 的适配层）
                                │ 当期 DSH；P2 WorkBuddy/GitHub
┌───────────────────────────────┼──────────────────────────────┐
│  产品：社媒增长 Agent 平台      │                              │
│                               │                              │
│  ① Agent 基座能力（9 项）      │                              │
│     工具调用 / 上下文 / 提示词 / 短期记忆 / 权限 / 可观测性     │
│     （复用 DSH 3 项 + 增量 6 项，见 §6.5）                     │
│  ② 业务壳（7 固定 Tab + 动态面板）                            │
│     对话 / 业务流库 / 调度 / 账户 / 成员 / 用量 / 数据面板      │
│  ③ 业务线（skill/mcp/workflow 资产包，可插拔）                │
│     广告归因（PT）/ 内容运营 / 未来更多                         │
└───────────────────────────────┼──────────────────────────────┘
                                ▼
┌──────────────────────────────────────────────────────────────┐
│  后端服务层：Workflow REST（Collect / 同步 / 查询 / 审计 / 模板） │
│  Collect = Webhook 异步幂等；查询 = P95<5s；同步 = 可调度        │
└──────────────────────────────────────────────────────────────┘
                                ▼
┌──────────────────────────────────────────────────────────────┐
│  存储：PostgreSQL（元数据/权限/会话/日志）                       │
│        ClickHouse（事件大数据，P1）· 向量库（RAG，P2）           │
└──────────────────────────────────────────────────────────────┘
                                ▼
┌──────────────────────────────────────────────────────────────┐
│  外部调度器（K8s CronJob / Temporal）→ DSH Headless            │
│  仅调度「需要拉」的业务流（广告同步、内容抓取等）                 │
└──────────────────────────────────────────────────────────────┘
```

### 12.2 开发基线（原型映射）

| 现有原型 | 内容 | 处理方式 |
|---|---|---|
| `growth-agent-platform` 合并原型 | Agent 壳、业务流库、调度、账户、内容下钻、归因面板入口 | 高保真基线；按 v1.5 补开始页、库内分区（编排\|单点能力）、命名、身份切换 |
| `powertokens-attribution-prototype` | 归因五 Tab | 作为「广告归因 · Token业态」产物视图 |

### 12.3 待补齐能力（按优先级）

| 能力 | 优先级 | 范围 |
|---|---|---|
| 开始页 + 新老用户默认落地 + 演示身份切换 | P0 | 第一期 + 高保真 |
| 业务流库：编排 \| 单点能力（Skill·MCP·Workflow）分区 UI | P0 | 高保真；P1 可先做编排+归因 |
| 数据面板动态 Tab | P0 | 第一期 |
| 归因启用向导（含 SS-GTM 清单） | P0 | 第一期 |
| 对话：单面板上下文 + @ + PT key | P0 | 第一期 |
| 调度与执行主层 | P0 | 第一期 |
| 账户授权分节（含 PT 双重身份） | P0 | 第一期 |
| 单点能力演示运行（Skill/MCP/Workflow 单独触发） | P0（高保真） | 高保真；非第一期验收 |
| 自定义定时次层占位 | P2 / 高保真 | 非第一期验收 |
| 内容线真实交付 | P2 | 非第一期 |
| Skill-1/2 产品化 | 演示 / 后期 | 非第一期 |
| 多面板上下文 | P1 | 对话增强 |
| 工具调用（skill/mcp/workflow → LLM Tool） | P0 | 第一期（agent 基座） |
| 短期记忆（会话上下文） | P0 | 第一期（agent 基座） |
| 用量与日志 Tab（工具调用日志 + token 计费） | P0 | 第一期（可观测性轻量） |
| RAG / 长期记忆 / 完整可观测性 | P2 | agent 基座 P2 项 |
| 第三方载体胶水 | P2 | 架构保留 |

### 12.4 明确不做（第一期）

- 交汇点完整归因打通（仅预留接口）
- A 类一键部署产品化、完整四维审计产品化、Shopify 适配
- 多租户商业化计费
- 内容运营线真实交付（高保真可演示）
- WorkBuddy/GitHub 等胶水实现
- 多面板上下文选择
- 通用 Agent 自定义 cron（仅占位）
- 广告写操作、面板直查 MCP、PT 业务库直连

---

## 附录 A：隐私与合规

> 处理社媒数据 + 广告数据的产品，隐私合规是上线硬性门槛。（自 v1.3 保留）

### A.1 数据存储与保留

| 数据类型 | 存储位置 | 保留期限 | 删除机制 |
|---|---|---|---|
| 社媒贴文/评论内容 | ClickHouse | 90 天 | 自动清理，用户可手动删除 |
| 广告账户数据（花费、转化） | PostgreSQL（加密） | 用户账户存续期 | 账户注销时 30 天内删除 |
| 用户对话历史 | PostgreSQL | 30 天 | 自动归档，超期删除 |
| 向量索引（RAG） | 向量库 | 与源数据同步 | 源数据删除时同步删除 |

### A.2 平台 ToS 风险

| 平台 | 风险点 | 缓解措施 |
|---|---|---|
| Reddit | 官方 API rate limit 严格 | 官方 API + 指数退避 |
| X (Twitter) | API 付费层级限制 | 仅读公开数据，遵守 tier |
| TikTok | 无官方 Research API | 第三方 MCP，明示来源 |
| Instagram | Graph API | 仅读授权范围内公开数据 |

### A.3 加密与审计

- **传输加密**：API 强制 TLS 1.3
- **存储加密**：广告账户数据 AES-256，密钥 KMS
- **访问审计**：可导出「谁查看了哪个广告账户」
- **数据隔离**：Workspace 级隔离

---

## 附录 B：第三方限流 / 限量调研（接入前置）

> 第三方 API / MCP 均有 rate limit、配额与 ToS 约束，**接入前必须单独调研确认**，输出「配额 + 速率 + 降级」结论作为接入决策依据。

| 第三方 | 限流/限量 | 要调研确认的点 |
|---|---|---|
| X API v2 | 免费档受限 | 免费/付费档请求配额、价格 |
| Reddit Data API | rate limit 严格 | 每分钟请求数、OAuth 配额 |
| TikTok | 无官方 Research API | 第三方 API 稳定性、签名、配额 |
| Instagram Graph API | 按应用配额 | 权限范围、速率限制 |
| Telegram Bot API | flood control | 每分钟消息数 |
| Meta Marketing API | 按 ad account | 只读权限、速率限制 |
| **Ryze MCP** | 第三方，未知 | 认证、限流、**降级方案** |

> **第一期（归因）重点调研：** Ryze MCP + Meta Marketing API（广告同步）；Collect 是 SS-GTM 主动推送（非我方拉取），限流问题小。
> **降级原则：** 任一第三方限流/失败 → 降级到备用源或明确空态提示，**禁止幻觉补数**（见 §9 铁律 2）。

---

> 本 PRD 是**产品形态与第一期边界**的开发基线。任何需求变更须改对应章节并更新决策表；口头变更视为无效。
> 归因尖刀的事件契约与分期验收细节，以 `PRODUCT-NORTH-STAR.md` / `PRODUCT-PHASES-SCOPE.md` 为准，并与本文 §1.7、§8.1、§9 对齐。
